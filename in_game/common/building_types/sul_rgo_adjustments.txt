# RGO Size Modifier Replacement — Event Buildings
# Cancels vanilla flat local_max_rgo_size, replaces with sul_rgo_size

TRY_INJECT:ori_portuguese_quarters = {
	modifier = { local_max_rgo_size = -3 sul_rgo_size = 3 }
}
