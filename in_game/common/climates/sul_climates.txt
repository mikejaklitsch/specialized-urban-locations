# Climate development modifier adjustments — convert percentage to flat

TRY_INJECT:arctic = {
	location_modifier = {
		local_monthly_development_modifier = 0.25 # nullify vanilla -0.25
		local_monthly_development = -0.030       # flat extraction push (eq -30)
	}
}

TRY_INJECT:tropical = {
	location_modifier = {
		local_monthly_development_modifier = 0.10 # nullify vanilla -0.10
		local_monthly_development = -0.002       # flat extraction push (eq -2)
	}
}
