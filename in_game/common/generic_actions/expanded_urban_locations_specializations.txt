# Expanded Urban Locations - Specialization Designation Actions
# These actions allow players to designate a specialization for towns/cities
# Costs gold + prestige, destroys non-matching buildings, applies modifier
# scope:actor = country, scope:target = location

designate_mining_specialization = {
	type = owncountry

	potential = {
	}

	allow = {
	}

	ai_tick = monthly
	ai_tick_frequency = 12

	ai_prerequisite = {
		at_war = no
		treasury >= 100
	}

	ai_will_do = {
		# Base inertia - AI resists changing specializations
		add = {
			desc = "inertia"
			value = -100
		}

		# Strong desire if location has mining resources but wrong specialization
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					has_mining_resources = yes
					has_any_specialization = yes
					NOT = { has_mining_specialization = yes }
				}
			}
			add = {
				desc = "resource_mismatch_correction"
				value = 150
			}
		}

		# Bonus for cities (higher value targets)
		if = {
			limit = {
				exists = scope:target
				scope:target = { location_rank = location_rank:city }
			}
			add = {
				desc = "city_priority"
				value = 25
			}
		}
	}

	price = price:designate_specialization_price

	select_trigger = {
		looking_for_a = location
		target_flag = target
		source = actor
		name = "designate_mining_select"

		visible = {
			owner = scope:actor
			is_rural_or_higher = yes
		}

		enabled = {
			has_mining_resources = yes
			NOT = { has_mining_specialization = yes }
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:target = {
				clear_all_specialization_modifiers = yes
				if = {
					limit = { location_rank = location_rank:rural_settlement }
					set_variable = { name = specialization_designation value = eul_mining_rural_value }
					add_location_modifier = { modifier = mining_rural_specialization }
				}
				else_if = {
					limit = { location_rank = location_rank:town }
					set_variable = { name = specialization_designation value = eul_mining_town_value }
					add_location_modifier = { modifier = mining_town_specialization }
				}
				else = {
					set_variable = { name = specialization_designation value = eul_mining_city_value }
					add_location_modifier = { modifier = mining_city_specialization }
				}
				destroy_non_mining_buildings = yes
			}
		}
	}
}

designate_farming_specialization = {
	type = owncountry

	potential = {
	}

	allow = {
	}

	ai_tick = monthly
	ai_tick_frequency = 12

	ai_prerequisite = {
		at_war = no
		treasury >= 100
	}

	ai_will_do = {
		# Base inertia - AI resists changing specializations
		add = {
			desc = "inertia"
			value = -100
		}

		# Strong desire if location has farming resources but wrong specialization
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					has_farming_resources = yes
					has_any_specialization = yes
					NOT = { has_farming_specialization = yes }
				}
			}
			add = {
				desc = "resource_mismatch_correction"
				value = 150
			}
		}

		# Bonus for cities (higher value targets)
		if = {
			limit = {
				exists = scope:target
				scope:target = { location_rank = location_rank:city }
			}
			add = {
				desc = "city_priority"
				value = 25
			}
		}
	}

	price = price:designate_specialization_price

	select_trigger = {
		looking_for_a = location
		target_flag = target
		source = actor
		name = "designate_farming_select"

		visible = {
			owner = scope:actor
			is_rural_or_higher = yes
		}

		enabled = {
			has_farming_resources = yes
			NOT = { has_farming_specialization = yes }
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:target = {
				clear_all_specialization_modifiers = yes
				if = {
					limit = { location_rank = location_rank:rural_settlement }
					set_variable = { name = specialization_designation value = eul_farming_rural_value }
					add_location_modifier = { modifier = farming_rural_specialization }
				}
				else_if = {
					limit = { location_rank = location_rank:town }
					set_variable = { name = specialization_designation value = eul_farming_town_value }
					add_location_modifier = { modifier = farming_town_specialization }
				}
				else = {
					set_variable = { name = specialization_designation value = eul_farming_city_value }
					add_location_modifier = { modifier = farming_city_specialization }
				}
				destroy_non_farming_buildings = yes
			}
		}
	}
}

designate_woodland_specialization = {
	type = owncountry

	potential = {
	}

	allow = {
	}

	ai_tick = monthly
	ai_tick_frequency = 12

	ai_prerequisite = {
		at_war = no
		treasury >= 100
	}

	ai_will_do = {
		# Base inertia - AI resists changing specializations
		add = {
			desc = "inertia"
			value = -100
		}

		# Strong desire if location has woodland resources but wrong specialization
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					has_woodland_resources = yes
					has_any_specialization = yes
					NOT = { has_woodland_specialization = yes }
				}
			}
			add = {
				desc = "resource_mismatch_correction"
				value = 150
			}
		}

		# Bonus for cities (higher value targets)
		if = {
			limit = {
				exists = scope:target
				scope:target = { location_rank = location_rank:city }
			}
			add = {
				desc = "city_priority"
				value = 25
			}
		}
	}

	price = price:designate_specialization_price

	select_trigger = {
		looking_for_a = location
		target_flag = target
		source = actor
		name = "designate_woodland_select"

		visible = {
			owner = scope:actor
			is_rural_or_higher = yes
		}

		enabled = {
			has_woodland_resources = yes
			NOT = { has_woodland_specialization = yes }
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:target = {
				clear_all_specialization_modifiers = yes
				if = {
					limit = { location_rank = location_rank:rural_settlement }
					set_variable = { name = specialization_designation value = eul_woodland_rural_value }
					add_location_modifier = { modifier = woodland_rural_specialization }
				}
				else_if = {
					limit = { location_rank = location_rank:town }
					set_variable = { name = specialization_designation value = eul_woodland_town_value }
					add_location_modifier = { modifier = woodland_town_specialization }
				}
				else = {
					set_variable = { name = specialization_designation value = eul_woodland_city_value }
					add_location_modifier = { modifier = woodland_city_specialization }
				}
				destroy_non_woodland_buildings = yes
			}
		}
	}
}

designate_gathering_specialization = {
	type = owncountry

	potential = {
	}

	allow = {
	}

	ai_tick = monthly
	ai_tick_frequency = 12

	ai_prerequisite = {
		at_war = no
		treasury >= 100
	}

	ai_will_do = {
		# Base inertia - AI resists changing specializations
		add = {
			desc = "inertia"
			value = -100
		}

		# Strong desire if location has gathering resources but wrong specialization
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					has_gathering_resources = yes
					has_any_specialization = yes
					NOT = { has_gathering_specialization = yes }
				}
			}
			add = {
				desc = "resource_mismatch_correction"
				value = 150
			}
		}

		# Bonus for cities (higher value targets)
		if = {
			limit = {
				exists = scope:target
				scope:target = { location_rank = location_rank:city }
			}
			add = {
				desc = "city_priority"
				value = 25
			}
		}
	}

	price = price:designate_specialization_price

	select_trigger = {
		looking_for_a = location
		target_flag = target
		source = actor
		name = "designate_gathering_select"

		visible = {
			owner = scope:actor
			is_rural_or_higher = yes
		}

		enabled = {
			has_gathering_resources = yes
			NOT = { has_gathering_specialization = yes }
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:target = {
				clear_all_specialization_modifiers = yes
				if = {
					limit = { location_rank = location_rank:rural_settlement }
					set_variable = { name = specialization_designation value = eul_gathering_rural_value }
					add_location_modifier = { modifier = gathering_rural_specialization }
				}
				else_if = {
					limit = { location_rank = location_rank:town }
					set_variable = { name = specialization_designation value = eul_gathering_town_value }
					add_location_modifier = { modifier = gathering_town_specialization }
				}
				else = {
					set_variable = { name = specialization_designation value = eul_gathering_city_value }
					add_location_modifier = { modifier = gathering_city_specialization }
				}
				destroy_non_gathering_buildings = yes
			}
		}
	}
}

designate_commercial_specialization = {
	type = owncountry

	potential = {
	}

	allow = {
	}

	ai_tick = monthly
	ai_tick_frequency = 12

	ai_prerequisite = {
		at_war = no
		treasury >= 100
	}

	ai_will_do = {
		# Base inertia - AI resists changing specializations
		add = {
			desc = "inertia"
			value = -100
		}

		# Strong desire if location is market center but not commercial
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					is_market_center = yes
					NOT = { has_commercial_specialization = yes }
				}
			}
			add = {
				desc = "market_center_should_be_commercial"
				value = 200
			}
		}

		# Moderate desire for high harbor locations
		if = {
			limit = {
				exists = scope:target
				scope:target = {
					modifier:harbor_suitability > 0.7
					NOT = { has_commercial_specialization = yes }
				}
			}
			add = {
				desc = "harbor_commercial_potential"
				value = 100
			}
		}

		# Bonus for cities (higher value targets)
		if = {
			limit = {
				exists = scope:target
				scope:target = { location_rank = location_rank:city }
			}
			add = {
				desc = "city_priority"
				value = 25
			}
		}
	}

	price = price:designate_specialization_price

	select_trigger = {
		looking_for_a = location
		target_flag = target
		source = actor
		name = "designate_commercial_select"

		visible = {
			owner = scope:actor
			is_rural_or_higher = yes
		}

		# Commercial has no resource requirement - but cannot already be commercial
		enabled = {
			NOT = { has_commercial_specialization = yes }
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:target = {
				clear_all_specialization_modifiers = yes
				if = {
					limit = { location_rank = location_rank:rural_settlement }
					set_variable = { name = specialization_designation value = eul_commercial_rural_value }
					add_location_modifier = { modifier = commercial_rural_specialization }
				}
				else_if = {
					limit = { location_rank = location_rank:town }
					set_variable = { name = specialization_designation value = eul_commercial_town_value }
					add_location_modifier = { modifier = commercial_town_specialization }
				}
				else = {
					set_variable = { name = specialization_designation value = eul_commercial_city_value }
					add_location_modifier = { modifier = commercial_city_specialization }
				}
				destroy_non_commercial_buildings = yes
			}
		}
	}
}
