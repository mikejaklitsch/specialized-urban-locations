REPLACE:relocate_market = {
	type = owncountry

	potential = {
	}

	allow = {
		custom_tooltip = {
			text = SUL_RELOCATE_MARKET_DISABLED
			always = no
		}
	}

	ai_tick = never

	select_trigger = {
		looking_for_a = market
		source = actor
		target_flag = target
		name = "relocate_market_select_market"
		none_available_msg_key = "relocate_market_no_markets"
		column = { data = name }

		visible = {
			always = no
		}
	}

	effect = { }
}
