# Per-market passthrough demand pulse. Iterates every_market_in_world once
# per weather tick, skipping unowned markets. Moved from monthly_country_pulse
# to avoid redundant per-country iterations over shared markets.

sul_passthrough_pulse = {
	effect = {
		sul_passthrough_apply = yes
	}
}
