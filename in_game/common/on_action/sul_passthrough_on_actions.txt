# Per-country passthrough demand pulse. Uses every_market_center_in_country
# from country scope so add_temporary_demand always runs on a valid owned
# market - every_market_in_world includes unowned markets where the call
# returns null.

sul_passthrough_pulse = {
	trigger = { has_markets = yes }

	effect = {
		sul_passthrough_apply = yes
	}
}
