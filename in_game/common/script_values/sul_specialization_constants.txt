# ============================================
# SPECIALIZATION DESIGNATION CONSTANTS - SCRIPT VALUES
# ============================================
# These values must match the defines in:
#   loading_screen/common/defines/expanded_urban_locations_specialization_defines.txt
#
# Number Mapping:
#   Rural (21-25): 21=mining, 22=farming, 23=gathering, 24=woodland, 25=commercial
#   Towns (1-5):   1=mining, 2=farming, 3=gathering, 4=woodland, 5=commercial
#   Cities (11-15): 11=mining, 12=farming, 13=gathering, 14=woodland, 15=commercial
#
# Usage in script: sul_mining_town_value (these script_values)
# Usage in GUI: hard-coded values (GetDefine doesn't work with mod namespaces)
# ============================================

# ============================================
# GOODS BASE PRICES (from game/in_game/common/goods/)
# Update these if base game changes default_market_price values
# ============================================

# Mining raw materials
# sul_iron_base = 3
# sul_stone_base = 1

# # Mining produced goods
# sul_tools_base = 3
# sul_weaponry_base = 3
# sul_steel_base = 5

# # Farming raw materials
# sul_wool_base = 2
# sul_fiber_crops_base = 1.5
# sul_fruit_base = 1.5
# sul_wine_base = 2

# # Farming produced goods
# sul_beer_base = 2
# sul_liquor_base = 2.5
# sul_leather_base = 3

# # Gathering raw materials
# sul_salt_base = 4
# sul_fish_base = 1
# sul_clay_base = 0.5

# # Gathering produced goods
# sul_porcelain_base = 3
# sul_pottery_base = 1

# # Woodland raw materials
# sul_lumber_base = 1.5
# sul_tar_base = 2
# sul_coal_base = 2

# # Woodland produced goods
# sul_furniture_base = 3
# sul_naval_supplies_base = 3

# # Commercial goods
# sul_cloth_base = 3
# sul_silk_base = 4
# sul_pepper_base = 5
# sul_gems_base = 4
# sul_dyes_base = 4

sul_building_cost_per_level = 0.1 # ai decision weight against respecializing per building level

# ============================================
# PRICE THRESHOLDS (computed from base prices)
# High demand: >1.5x base price
# Surplus (commercial): <0.75x base price
# ============================================

# Mining high demand thresholds (1.5x base)
sul_iron_high = { value = "default_price(goods:iron)" multiply = 1.5 }
sul_stone_high = { value = "default_price(goods:stone)" multiply = 1.5 }
sul_tools_high = { value = "default_price(goods:tools)" multiply = 1.5 }
sul_weaponry_high = { value = "default_price(goods:weaponry)" multiply = 1.5 }
sul_steel_high = { value = "default_price(goods:steel)" multiply = 1.5 }
# Farming high demand thresholds (1.5x base)
sul_wool_high = { value = "default_price(goods:wool)" multiply = 1.5 }
sul_fiber_crops_high = { value = "default_price(goods:fiber_crops)" multiply = 1.5 }
sul_fruit_high = { value = "default_price(goods:fruit)" multiply = 1.5 }
sul_wine_high = { value = "default_price(goods:wine)" multiply = 1.5 }
sul_beer_high = { value = "default_price(goods:beer)" multiply = 1.5 }
sul_liquor_high = { value = "default_price(goods:liquor)" multiply = 1.5 }
sul_leather_high = { value = "default_price(goods:leather)" multiply = 1.5 }

# Gathering high demand thresholds (1.5x base)
sul_salt_high = { value = "default_price(goods:salt)" multiply = 1.5 }
sul_fish_high = { value = "default_price(goods:fish)" multiply = 1.5 }
sul_clay_high = { value = "default_price(goods:clay)" multiply = 1.5 }
sul_porcelain_high = { value = "default_price(goods:porcelain)" multiply = 1.5 }
sul_pottery_high = { value = "default_price(goods:pottery)" multiply = 1.5 }

# Woodland high demand thresholds (1.5x base)
sul_lumber_high = { value = "default_price(goods:lumber)" multiply = 1.5 }
sul_tar_high = { value = "default_price(goods:tar)" multiply = 1.5 }
sul_coal_high = { value = "default_price(goods:coal)" multiply = 1.5 }
sul_furniture_high = { value = "default_price(goods:furniture)" multiply = 1.5 }
sul_naval_supplies_high = { value = "default_price(goods:naval_supplies)" multiply = 1.5 }
# Commercial surplus thresholds (0.75x base - low prices)
sul_wheat_low = { value = "default_price(goods:wheat)" multiply = 0.75 }
sul_iron_low = { value = "default_price(goods:iron)" multiply = 0.75 }
sul_lumber_low = { value = "default_price(goods:lumber)" multiply = 0.75 }
sul_cloth_low = { value = "default_price(goods:cloth)" multiply = 0.75 }
sul_wine_low = { value = "default_price(goods:wine)" multiply = 0.75 }
sul_silk_low = { value = "default_price(goods:silk)" multiply = 0.75 }
sul_pepper_low = { value = "default_price(goods:pepper)" multiply = 0.75 }
sul_gems_low = { value = "default_price(goods:gems)" multiply = 0.75 }
sul_dyes_low = { value = "default_price(goods:dyes)" multiply = 0.75 }

# ============================================
# SPECIALIZATION DESIGNATION VALUES
# ============================================

# Rural specialization values
sul_mining_rural_value = 21
sul_farming_rural_value = 22
sul_gathering_rural_value = 23
sul_woodland_rural_value = 24
sul_commercial_rural_value = 25

# Town specialization values
sul_mining_town_value = 1
sul_farming_town_value = 2
sul_gathering_town_value = 3
sul_woodland_town_value = 4
sul_commercial_town_value = 5

# City specialization values
sul_mining_city_value = 11
sul_farming_city_value = 12
sul_gathering_city_value = 13
sul_woodland_city_value = 14
sul_commercial_city_value = 15

# ============================================
# DIVERSITY PRESSURE (percentage-based, cached)
# ============================================
# Uses cached country variables updated only when specializations change
# Variables: var:sul_mining_count, var:sul_farming_count, etc.
# var:sul_total_weighted = farming×2 + others×1
# Result is positive if under-represented, negative if over-represented
# Returns 0 if variables not yet initialized (first action by this country)

# Mining diversity pressure: (expected - actual) / expected × 5
sul_mining_diversity = {
	if = {
		limit = { NOT = { has_variable = sul_total_weighted } }
		value = 0
	}
	else = {
		value = var:sul_total_weighted
		divide = 6
		subtract = var:sul_mining_count
		multiply = 6
		divide = { value = var:sul_total_weighted min = 1 }
		multiply = 5
	}
}

# Farming diversity pressure: expected = total_weighted / 3
sul_farming_diversity = {
	if = {
		limit = { NOT = { has_variable = sul_total_weighted } }
		value = 0
	}
	else = {
		value = var:sul_total_weighted
		divide = 3
		subtract = var:sul_farming_count
		multiply = 3
		divide = { value = var:sul_total_weighted min = 1 }
		multiply = 5
	}
}

# Woodland diversity pressure
sul_woodland_diversity = {
	if = {
		limit = { NOT = { has_variable = sul_total_weighted } }
		value = 0
	}
	else = {
		value = var:sul_total_weighted
		divide = 6
		subtract = var:sul_woodland_count
		multiply = 6
		divide = { value = var:sul_total_weighted min = 1 }
		multiply = 5
	}
}

# Gathering diversity pressure
sul_gathering_diversity = {
	if = {
		limit = { NOT = { has_variable = sul_total_weighted } }
		value = 0
	}
	else = {
		value = var:sul_total_weighted
		divide = 6
		subtract = var:sul_gathering_count
		multiply = 6
		divide = { value = var:sul_total_weighted min = 1 }
		multiply = 5
	}
}

# Commercial diversity pressure
sul_commercial_diversity = {
	if = {
		limit = { NOT = { has_variable = sul_total_weighted } }
		value = 0
	}
	else = {
		value = var:sul_total_weighted
		divide = 6
		subtract = var:sul_commercial_count
		multiply = 6
		divide = { value = var:sul_total_weighted min = 1 }
		multiply = 5
	}
}

# ============================================
# MARKET SUPPLY/DEMAND PRESSURE (for AI weights)
# ============================================
# These calculate (demand/supply - 1) × scale
# Positive = shortage (demand > supply), encourages production
# Negative = surplus (supply > demand), discourages production
# Uses min = 0.1 on supply to avoid divide-by-zero
# Clamped to ±1 before scaling to prevent extreme swings
# Scale factor of 5 gives max ±5 AI weight impact

# Mining: Iron market pressure (called from market scope)
sul_iron_market_pressure = {
	value = "goods_demand_in_market(goods:iron)"
	divide = {
		value = "goods_supply_in_market(goods:iron)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# Farming: Wool market pressure
sul_wool_market_pressure = {
	value = "goods_demand_in_market(goods:wool)"
	divide = {
		value = "goods_supply_in_market(goods:wool)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# Farming: Wheat market pressure (food staple)
sul_wheat_market_pressure = {
	value = "goods_demand_in_market(goods:wheat)"
	divide = {
		value = "goods_supply_in_market(goods:wheat)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# Woodland: Lumber market pressure
sul_lumber_market_pressure = {
	value = "goods_demand_in_market(goods:lumber)"
	divide = {
		value = "goods_supply_in_market(goods:lumber)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# Gathering: Salt market pressure
sul_salt_market_pressure = {
	value = "goods_demand_in_market(goods:salt)"
	divide = {
		value = "goods_supply_in_market(goods:salt)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# Gathering: Clay market pressure
sul_clay_market_pressure = {
	value = "goods_demand_in_market(goods:clay)"
	divide = {
		value = "goods_supply_in_market(goods:clay)"
		min = 0.1
	}
	subtract = 1
	max = 1
	min = -1
	multiply = 5
}

# ============================================
# PRICE BONUS SCRIPT VALUES (market scope)
# Formula: (price / base_price - 1) × 8, capped at -8 to +16
# Positive = shortage (high prices), negative = surplus (low prices)
# ============================================

# Mining raw: iron price bonus
sul_mining_raw_price_bonus = {
	value = "market_price(goods:iron)"
	divide = "default_price(goods:iron)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Mining finished: tools price bonus
sul_mining_finished_price_bonus = {
	value = "market_price(goods:tools)"
	divide = "default_price(goods:tools)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Farming raw: wool price bonus
sul_farming_raw_price_bonus = {
	value = "market_price(goods:wool)"
	divide = "default_price(goods:wool)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Farming finished: leather price bonus
sul_farming_finished_price_bonus = {
	value = "market_price(goods:leather)"
	divide = "default_price(goods:leather)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Woodland raw: lumber price bonus
sul_woodland_raw_price_bonus = {
	value = "market_price(goods:lumber)"
	divide = "default_price(goods:lumber)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Woodland finished: furniture price bonus
sul_woodland_finished_price_bonus = {
	value = "market_price(goods:furniture)"
	divide = "default_price(goods:furniture)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Gathering raw: salt price bonus
sul_gathering_raw_price_bonus = {
	value = "market_price(goods:salt)"
	divide = "default_price(goods:salt)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

# Gathering finished: pottery price bonus
sul_gathering_finished_price_bonus = {
	value = "market_price(goods:pottery)"
	divide = "default_price(goods:pottery)"
	subtract = 1
	multiply = 8
	min = -8
	max = 16
}

