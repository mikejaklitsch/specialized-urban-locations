# Demand Tooltip Per Pop — location scope.
# GDP-based wages + local asset returns + national estate returns - provisions cost.

@asset_return_rate = 0.05
@estate_return_rate = 0.025
@provisions_food_value = 30

@food_nobles = 24.0
@food_clergy = 6.0
@food_burghers = 6.0
@food_soldiers = 4.5
@food_laborers = 1.5

@commoner_weight_soldiers = 12
@commoner_weight_laborers = 4
@commoner_weight_peasants = 1

# --- WAGE COMPONENT PER POP (GDP wage pool × power / pops) ---

sul_noble_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:nobles > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:nobles_estate)"
			divide = num_pop_type:nobles
		}
	}
}

sul_clergy_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:clergy > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:clergy_estate)"
			divide = num_pop_type:clergy
		}
	}
}

sul_burgher_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:burghers > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:burghers_estate)"
			divide = num_pop_type:burghers
		}
	}
}

sul_commoner_weighted_total = {
	value = { value = num_pop_type:soldiers multiply = @commoner_weight_soldiers }

	add = { value = num_pop_type:laborers multiply = @commoner_weight_laborers }
	add = { value = num_pop_type:peasants multiply = @commoner_weight_peasants }

	min = 0.001
}

sul_soldier_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:soldiers > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_soldiers
			divide = sul_commoner_weighted_total
		}
	}
}

sul_laborer_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:laborers > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_laborers
			divide = sul_commoner_weighted_total
		}
	}
}

sul_peasant_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:peasants > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_peasants
			divide = sul_commoner_weighted_total
		}
	}
}

# --- WAGE SHARE DISPLAY ---

@dev_midpoint = 0.50
@emp_midpoint = 0.50

sul_wage_share_display = {
	value = var:sul_wage_share
}

sul_wage_share_pct = {
	value = var:sul_wage_share
	multiply = 100
}

sul_wage_share_dev_factor = {
	value = 2

	multiply = { value = development divide = 100 }

	divide = { value = @dev_midpoint add = { value = development divide = 100 } }
}

sul_wage_share_emp_factor = {
	value = 2
	multiply = employment_percentage

	divide = { value = @emp_midpoint add = employment_percentage }
}

sul_wage_share_market_access = {
	value = market_access
}

sul_wage_share_dev_pct = {
	value = development
}

sul_wage_share_emp_pct = {
	value = employment_percentage
	multiply = 100
}

# --- GDP WAGE DISPLAY ---

sul_noble_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_noble_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:nobles_estate)"
}

sul_noble_local_wage_modifier = {
	value = 0
}

sul_noble_total_wage_modifier = {
	value = 0
}

sul_clergy_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_clergy_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:clergy_estate)"
}

sul_clergy_local_wage_modifier = {
	value = 0
}

sul_clergy_total_wage_modifier = {
	value = 0
}

sul_burgher_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_burgher_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:burghers_estate)"
}

sul_burgher_local_wage_modifier = {
	value = 0
}

sul_burgher_total_wage_modifier = {
	value = 0
}

sul_soldier_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_soldier_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:peasants_estate)"
}

sul_soldier_local_wage_modifier = {
	value = 0
}

sul_soldier_total_wage_modifier = {
	value = 0
}

sul_laborer_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_laborer_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:peasants_estate)"
}

sul_laborer_local_wage_modifier = {
	value = 0
}

sul_laborer_total_wage_modifier = {
	value = 0
}

sul_peasant_base_wage = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_peasant_global_wage_modifier = {
	value = "local_relative_estate_power(estate_type:peasants_estate)"
}

sul_peasant_local_wage_modifier = {
	value = 0
}

sul_peasant_total_wage_modifier = {
	value = 0
}

# --- POP CAPACITY WAGE FACTOR (legacy — no longer affects wages) ---

sul_noble_pop_cap_wage = {
	value = 0
}

sul_clergy_pop_cap_wage = {
	value = 0
}

sul_burgher_pop_cap_wage = {
	value = 0
}

sul_soldier_pop_cap_wage = {
	value = 0
}

sul_laborer_pop_cap_wage = {
	value = 0
}

sul_peasant_pop_cap_wage = {
	value = 0
}

# --- DISPLAY HELPERS ---

sul_display_pop_capacity_pct = {
	value = location_population_percentage
	multiply = 100
}

sul_display_enfranchisement_pct = {
	value = modifier:local_peasant_enfranchisment
	add = owner.modifier:global_peasant_enfranchisment
	min = 0.1
	max = 1.0
	multiply = 100
}

# --- ENFRANCHISEMENT WAGE FACTOR (legacy — kept for GUI compat) ---

sul_commoner_enfr_wage = {
	value = 0
}

# --- DEMAND PER POP (GDP wages + asset returns + national returns - provisions) ---

sul_noble_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:nobles > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:nobles_estate)"
			divide = num_pop_type:nobles

			add = {
				value = owner.var:sul_estate_gold_nobles
				multiply = @estate_return_rate
				divide = { value = owner.var:sul_country_pop_nobles min = 1 }
			}

			subtract = sul_noble_provisions_cost

			min = 0
		}
	}
}

sul_clergy_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:clergy > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:clergy_estate)"
			divide = num_pop_type:clergy

			add = {
				value = owner.var:sul_estate_gold_clergy
				multiply = @estate_return_rate
				divide = { value = owner.var:sul_country_pop_clergy min = 1 }
			}

			subtract = sul_clergy_provisions_cost

			min = 0
		}
	}
}

sul_burgher_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:burghers > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:burghers_estate)"
			divide = num_pop_type:burghers

			add = {
				value = owner.var:sul_estate_gold_burghers
				multiply = @estate_return_rate
				divide = { value = owner.var:sul_country_pop_burghers min = 1 }
			}

			subtract = sul_burgher_provisions_cost

			min = 0
		}
	}
}

sul_soldier_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:soldiers > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_soldiers
			divide = sul_commoner_weighted_total

			add = {
				value = owner.var:sul_estate_gold_peasants
				multiply = @estate_return_rate

				divide = {
					value = owner.var:sul_country_pop_soldiers
					add = owner.var:sul_country_pop_laborers
					add = owner.var:sul_country_pop_peasants
					min = 1
				}
			}

			subtract = sul_soldier_provisions_cost

			min = 0
		}
	}
}

sul_laborer_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:laborers > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_laborers
			divide = sul_commoner_weighted_total

			add = {
				value = owner.var:sul_estate_gold_peasants
				multiply = @estate_return_rate

				divide = {
					value = owner.var:sul_country_pop_soldiers
					add = owner.var:sul_country_pop_laborers
					add = owner.var:sul_country_pop_peasants
					min = 1
				}
			}

			subtract = sul_laborer_provisions_cost

			min = 0
		}
	}
}

sul_peasant_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:peasants > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_peasants
			divide = sul_commoner_weighted_total

			add = {
				value = owner.var:sul_estate_gold_peasants
				multiply = @estate_return_rate

				divide = {
					value = owner.var:sul_country_pop_soldiers
					add = owner.var:sul_country_pop_laborers
					add = owner.var:sul_country_pop_peasants
					min = 1
				}
			}

			min = 0
		}
	}
}

sul_tribesmen_demand_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:tribesmen > 0 }
		add = {
			value = var:sul_local_gdp
			multiply = var:sul_wage_share
			add = { value = var:sul_local_assets multiply = @asset_return_rate }
			multiply = "local_relative_estate_power(estate_type:tribes_estate)"
			divide = { value = num_pop_type:tribesmen min = 0.001 }

			add = {
				value = owner.var:sul_estate_gold_tribes
				multiply = @estate_return_rate
				divide = { value = owner.var:sul_country_pop_tribesmen min = 1 }
			}

			min = 0
		}
	}
}

sul_tribesmen_wage_rate = {
	value = 0

	if = {
		limit = { num_pop_type:tribesmen > 0 }
		add = {
			value = var:sul_local_wages
			multiply = "local_relative_estate_power(estate_type:tribes_estate)"
			divide = num_pop_type:tribesmen
		}
	}
}

sul_tribesmen_capital_return = {
	value = 0

	if = {
		limit = { has_variable = sul_local_assets num_pop_type:tribesmen > 0 }
		add = {
			value = var:sul_local_assets
			multiply = @asset_return_rate
			multiply = "local_relative_estate_power(estate_type:tribes_estate)"
			divide = { value = num_pop_type:tribesmen min = 0.001 }
		}
	}
}

sul_tribesmen_power_share = {
	value = "local_relative_estate_power(estate_type:tribes_estate)"
}

# --- ENRICHMENT PER POP (savings tier, live-computed) ---

@sul_enrichment_weight = 0.53

sul_noble_enrichment_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:nobles > 0 }
		add = {
			value = "variable_map(sul_demand_base|pop_type:nobles)"
			multiply = "variable_map(sul_wealth_per_pop|pop_type:nobles)"
			multiply = @sul_enrichment_weight
			multiply = owner.var:sul_enrich_rate_nobles
		}
	}
}

sul_clergy_enrichment_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:clergy > 0 }
		add = {
			value = "variable_map(sul_demand_base|pop_type:clergy)"
			multiply = "variable_map(sul_wealth_per_pop|pop_type:clergy)"
			multiply = @sul_enrichment_weight
			multiply = owner.var:sul_enrich_rate_clergy
		}
	}
}

sul_burgher_enrichment_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:burghers > 0 }
		add = {
			value = "variable_map(sul_demand_base|pop_type:burghers)"
			multiply = "variable_map(sul_wealth_per_pop|pop_type:burghers)"
			multiply = @sul_enrichment_weight
			multiply = owner.var:sul_enrich_rate_burghers
		}
	}
}

sul_commoner_enrichment_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:peasants > 0 }
		add = {
			value = "variable_map(sul_demand_base|pop_type:peasants)"
			multiply = "variable_map(sul_wealth_per_pop|pop_type:peasants)"
			multiply = @sul_enrichment_weight
			multiply = owner.var:sul_enrich_rate_commoners
		}
	}
}

sul_tribesmen_enrichment_per_pop = {
	value = 0

	if = {
		limit = { num_pop_type:tribesmen > 0 }
		add = {
			value = "variable_map(sul_demand_base|pop_type:tribesmen)"
			multiply = "variable_map(sul_wealth_per_pop|pop_type:tribesmen)"
			multiply = @sul_enrichment_weight
			multiply = owner.var:sul_enrich_rate_tribesmen
		}
	}
}

# Enrichment rate modifiers (for tooltip hover)

sul_noble_enrichment_rate_display = {
	value = owner.modifier:sul_nobles_enrichment_rate
}

sul_clergy_enrichment_rate_display = {
	value = owner.modifier:sul_clergy_enrichment_rate
}

sul_burgher_enrichment_rate_display = {
	value = owner.modifier:sul_burghers_enrichment_rate
}

sul_commoner_enrichment_rate_display = {
	value = owner.modifier:sul_commoners_enrichment_rate
}

sul_tribesmen_enrichment_rate_display = {
	value = owner.modifier:sul_tribesmen_enrichment_rate
}

# --- WAGE BREAKDOWN COMPONENTS (for tooltip hover) ---

sul_wage_pool_total = {
	value = var:sul_local_wages
}

sul_commoner_power_share = {
	value = "local_relative_estate_power(estate_type:peasants_estate)"
}

# Commoner bill pct — weighted share of estate pool

sul_soldier_bill_pct = {
	value = { value = num_pop_type:soldiers multiply = @commoner_weight_soldiers }

	divide = sul_commoner_weighted_total
}

sul_laborer_bill_pct = {
	value = { value = num_pop_type:laborers multiply = @commoner_weight_laborers }

	divide = sul_commoner_weighted_total
}

sul_peasant_bill_pct = {
	value = { value = num_pop_type:peasants multiply = @commoner_weight_peasants }

	divide = sul_commoner_weighted_total
}

sul_peasants_bill_pct = {
	value = sul_peasant_bill_pct
}

# --- POP COUNTS (for tooltip display) ---

sul_pop_count_nobles = {
	value = num_pop_type:nobles
}

sul_pop_count_clergy = {
	value = num_pop_type:clergy
}

sul_pop_count_burghers = {
	value = num_pop_type:burghers
}

sul_pop_count_soldiers = {
	value = num_pop_type:soldiers
}

sul_pop_count_laborers = {
	value = num_pop_type:laborers
}

sul_pop_count_peasants = {
	value = num_pop_type:peasants
}

sul_pop_count_commoners = {
	value = num_pop_type:soldiers
	add = num_pop_type:laborers
	add = num_pop_type:peasants
}

sul_pop_count_tribesmen = {
	value = num_pop_type:tribesmen
}

# --- CAPITAL RETURNS PER POP (from aggregate local assets × power share) ---

sul_noble_capital_return = {
	value = var:sul_local_assets
	multiply = @asset_return_rate
	multiply = "local_relative_estate_power(estate_type:nobles_estate)"

	divide = { value = num_pop_type:nobles min = 0.001 }
}

sul_clergy_capital_return = {
	value = var:sul_local_assets
	multiply = @asset_return_rate
	multiply = "local_relative_estate_power(estate_type:clergy_estate)"

	divide = { value = num_pop_type:clergy min = 0.001 }
}

sul_burgher_capital_return = {
	value = var:sul_local_assets
	multiply = @asset_return_rate
	multiply = "local_relative_estate_power(estate_type:burghers_estate)"

	divide = { value = num_pop_type:burghers min = 0.001 }
}

sul_commoner_capital_return = {
	value = 0

	if = {
		limit = { num_pop_type:peasants > 0 }
		add = {
			value = var:sul_local_assets
			multiply = @asset_return_rate
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_peasants
			divide = sul_commoner_weighted_total
		}
	}
}

sul_soldier_capital_return = {
	value = 0

	if = {
		limit = { num_pop_type:soldiers > 0 }
		add = {
			value = var:sul_local_assets
			multiply = @asset_return_rate
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_soldiers
			divide = sul_commoner_weighted_total
		}
	}
}

sul_laborer_capital_return = {
	value = 0

	if = {
		limit = { num_pop_type:laborers > 0 }
		add = {
			value = var:sul_local_assets
			multiply = @asset_return_rate
			multiply = "local_relative_estate_power(estate_type:peasants_estate)"
			multiply = @commoner_weight_laborers
			divide = sul_commoner_weighted_total
		}
	}
}

# --- POLITICAL POWER SHARE (for wage distribution tooltip) ---

sul_noble_power_share = {
	value = "local_relative_estate_power(estate_type:nobles_estate)"
}

sul_clergy_power_share = {
	value = "local_relative_estate_power(estate_type:clergy_estate)"
}

sul_burgher_power_share = {
	value = "local_relative_estate_power(estate_type:burghers_estate)"
}

# --- PROVISIONS COST DISPLAY ---

sul_provisions_price_display = {
	value = "global_variable_map(sul_provisions_price_cache|market)"
}

sul_gross_wage_pool_display = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

sul_provisions_deducted_display = {
	value = 0
}

sul_noble_provisions_cost = {
	value = "global_variable_map(sul_prov_cost_nobles|market)"
}

sul_clergy_provisions_cost = {
	value = "global_variable_map(sul_prov_cost_clergy|market)"
}

sul_burgher_provisions_cost = {
	value = "global_variable_map(sul_prov_cost_burghers|market)"
}

# Provisions demand QUANTITY per 1k pops (food_consumption / 30, constant)

sul_noble_provisions_qty = {
	value = @[24/30]
}

sul_clergy_provisions_qty = {
	value = @[6/30]
}

sul_burgher_provisions_qty = {
	value = @[6/30]
}

sul_soldier_provisions_qty = {
	value = @[4.5/30]
}

sul_laborer_provisions_qty = {
	value = @[1.5/30]
}

sul_soldier_provisions_cost = {
	value = "global_variable_map(sul_prov_cost_soldiers|market)"
}

sul_laborer_provisions_cost = {
	value = "global_variable_map(sul_prov_cost_laborers|market)"
}

sul_net_wage_pool_display = {
	value = var:sul_local_gdp
	multiply = var:sul_wage_share
}

# --- LOCAL ASSETS DISPLAY ---

sul_local_assets_display = {
	value = var:sul_local_assets
}

sul_local_assets_max = {
	value = var:sul_local_gdp
	multiply = 5

	multiply = { value = 1 add = { value = prosperity divide = 100 } }

	min = 0
}

sul_local_assets_delta = {
	value = var:sul_asset_delta
}

sul_local_assets_gdp = {
	value = var:sul_local_gdp
}

sul_local_assets_prosperity_pct = {
	value = prosperity
}

sul_local_assets_prosperity_factor = {
	value = 1

	add = { value = prosperity divide = 100 }
}

# --- NATIONAL RETURN PER POP (estate_gold × 2.5% / national_pops) ---

sul_noble_national_return = {
	value = owner.var:sul_estate_gold_nobles
	multiply = @estate_return_rate

	divide = { value = owner.var:sul_country_pop_nobles min = 1 }
}

sul_clergy_national_return = {
	value = owner.var:sul_estate_gold_clergy
	multiply = @estate_return_rate

	divide = { value = owner.var:sul_country_pop_clergy min = 1 }
}

sul_burgher_national_return = {
	value = owner.var:sul_estate_gold_burghers
	multiply = @estate_return_rate

	divide = { value = owner.var:sul_country_pop_burghers min = 1 }
}

sul_commoner_national_return = {
	value = owner.var:sul_estate_gold_peasants
	multiply = @estate_return_rate

	divide = {
		value = owner.var:sul_country_pop_soldiers
		add = owner.var:sul_country_pop_laborers
		add = owner.var:sul_country_pop_peasants
		min = 1
	}
}

sul_tribesmen_national_return = {
	value = owner.var:sul_estate_gold_tribes
	multiply = @estate_return_rate

	divide = { value = owner.var:sul_country_pop_tribesmen min = 1 }
}

# --- NATIONAL RETURN DISPLAY (estate treasury + national pops, for tooltip hover) ---

sul_estate_treasury_nobles = {
	value = owner.var:sul_estate_gold_nobles
}

sul_estate_treasury_clergy = {
	value = owner.var:sul_estate_gold_clergy
}

sul_estate_treasury_burghers = {
	value = owner.var:sul_estate_gold_burghers
}

sul_estate_treasury_peasants = {
	value = owner.var:sul_estate_gold_peasants
}

sul_estate_treasury_tribes = {
	value = owner.var:sul_estate_gold_tribes
}

sul_national_pop_nobles = {
	value = owner.var:sul_country_pop_nobles
}

sul_national_pop_clergy = {
	value = owner.var:sul_country_pop_clergy
}

sul_national_pop_burghers = {
	value = owner.var:sul_country_pop_burghers
}

sul_national_pop_commoners = {
	value = owner.var:sul_country_pop_soldiers
	add = owner.var:sul_country_pop_laborers
	add = owner.var:sul_country_pop_peasants
}

sul_national_pop_tribesmen = {
	value = owner.var:sul_country_pop_tribesmen
}

sul_local_assets_gap = {
	value = sul_local_assets_max
	subtract = var:sul_local_assets
}

sul_local_assets_converge_step = {
	value = sul_local_assets_max
	multiply = 0.05
}

# --- TOTAL RETURNS (local capital + national estate, for tooltip summary) ---

sul_noble_total_return = {
	value = sul_noble_capital_return
	add = sul_noble_national_return
}

sul_clergy_total_return = {
	value = sul_clergy_capital_return
	add = sul_clergy_national_return
}

sul_burgher_total_return = {
	value = sul_burgher_capital_return
	add = sul_burgher_national_return
}

sul_commoner_total_return = {
	value = sul_commoner_capital_return
	add = sul_commoner_national_return
}

sul_soldier_total_return = {
	value = sul_soldier_capital_return
	add = sul_commoner_national_return
}

sul_laborer_total_return = {
	value = sul_laborer_capital_return
	add = sul_commoner_national_return
}

sul_tribesmen_total_return = {
	value = sul_tribesmen_capital_return
	add = sul_tribesmen_national_return
}

# --- ENRICHMENT DISPLAY ---

sul_estate_enrichment_display = {
	value = owner.modifier:estate_enrichment
}

sul_noble_enrichment_combined = {
	value = 1
	add = owner.modifier:estate_enrichment

	multiply = { value = 1 add = owner.modifier:sul_nobles_enrichment_rate }
}

sul_clergy_enrichment_combined = {
	value = 1
	add = owner.modifier:estate_enrichment

	multiply = { value = 1 add = owner.modifier:sul_clergy_enrichment_rate }
}

sul_burgher_enrichment_combined = {
	value = 1
	add = owner.modifier:estate_enrichment

	multiply = { value = 1 add = owner.modifier:sul_burghers_enrichment_rate }
}

sul_commoner_enrichment_combined = {
	value = 1
	add = owner.modifier:estate_enrichment

	multiply = { value = 1 add = owner.modifier:sul_commoners_enrichment_rate }
}

sul_tribesmen_enrichment_combined = {
	value = 1
	add = owner.modifier:estate_enrichment

	multiply = { value = 1 add = owner.modifier:sul_tribesmen_enrichment_rate }
}

# --- WAGE POOL BREAKDOWN (per-estate contribution, for nested tooltip) ---

sul_wage_bill_nobles_display = {
	value = var:sul_wage_bill_nobles
}

sul_wage_bill_clergy_display = {
	value = var:sul_wage_bill_clergy
}

sul_wage_bill_burghers_display = {
	value = var:sul_wage_bill_burghers
}

sul_wage_bill_soldiers_display = {
	value = var:sul_wage_bill_commoners
	multiply = sul_soldier_bill_pct
}

sul_wage_bill_laborers_display = {
	value = var:sul_wage_bill_commoners
	multiply = sul_laborer_bill_pct
}

sul_wage_bill_peasants_display = {
	value = var:sul_wage_bill_commoners
	multiply = sul_peasant_bill_pct
}

# --- COMMONER FLAT WEALTH (from aggregate local assets) ---

sul_commoner_flat_wealth = {
	value = var:sul_local_assets
	multiply = @asset_return_rate
	multiply = "local_relative_estate_power(estate_type:peasants_estate)"
}

# --- MINORITY RETURNS (legacy — per-estate minority assets removed) ---
# Minority estates now pull from aggregate pool via power share like everyone else.

sul_minority_total_weight = {
	value = 1
}

sul_noble_weight_share = {
	value = 0
}

sul_clergy_weight_share = {
	value = 0
}

sul_burgher_weight_share = {
	value = 0
}

sul_commoner_weight_share = {
	value = 0
}

sul_tribesmen_weight_share = {
	value = 0
}

sul_noble_dhimmi_return = {
	value = 0
}

sul_clergy_dhimmi_return = {
	value = 0
}

sul_burgher_dhimmi_return = {
	value = 0
}

sul_commoner_dhimmi_return = {
	value = 0
}

sul_tribesmen_dhimmi_return = {
	value = 0
}

sul_tribesmen_cossacks_return = {
	value = 0
}

# --- LEGACY ALIASES (referenced by GUI) ---

sul_noble_per_capita = {
	value = sul_noble_demand_per_pop
}

sul_clergy_per_capita = {
	value = sul_clergy_demand_per_pop
}

sul_burgher_per_capita = {
	value = sul_burgher_demand_per_pop
}

sul_commoner_per_capita = {
	value = sul_peasant_demand_per_pop
}

sul_noble_share_pct = {
	value = 0
}

sul_clergy_share_pct = {
	value = 0
}

sul_burgher_share_pct = {
	value = 0
}

sul_soldier_share_pct = {
	value = 0
}

sul_laborer_share_pct = {
	value = 0
}

sul_peasant_share_pct = {
	value = 0
}

sul_noble_tax_per_pop = {
	value = 0
}

sul_clergy_tax_per_pop = {
	value = 0
}

sul_burgher_tax_per_pop = {
	value = 0
}

sul_soldier_tax_per_pop = {
	value = 0
}

sul_laborer_tax_per_pop = {
	value = 0
}

sul_peasant_tax_per_pop = {
	value = 0
}

sul_noble_effective_share = {
	value = 0
}

sul_clergy_effective_share = {
	value = 0
}

sul_burgher_effective_share = {
	value = 0
}

sul_commoner_effective_share = {
	value = 0
}

sul_noble_tax_income = {
	value = 0
}

sul_clergy_tax_income = {
	value = 0
}

sul_burgher_tax_income = {
	value = 0
}

sul_soldier_tax_income = {
	value = 0
}

sul_laborer_tax_income = {
	value = 0
}

sul_peasant_tax_income = {
	value = 0
}

sul_noble_rents_wages = {
	value = sul_noble_demand_per_pop
}

sul_clergy_rents_wages = {
	value = sul_clergy_demand_per_pop
}

sul_burgher_rents_wages = {
	value = sul_burgher_demand_per_pop
}

sul_soldier_rents_wages = {
	value = sul_soldier_demand_per_pop
}

sul_laborer_rents_wages = {
	value = sul_laborer_demand_per_pop
}

sul_peasant_rents_wages = {
	value = sul_peasant_demand_per_pop
}

# --- DHIMMI / COSSACKS / TRIBES (legacy zero stubs) ---

sul_dhimmi_share_pct = {
	value = 0
}

sul_dhimmi_tax_income = {
	value = 0
}

sul_dhimmi_rents_wages = {
	value = 0
}

sul_cossacks_share_pct = {
	value = 0
}

sul_cossacks_tax_income = {
	value = 0
}

sul_cossacks_rents_wages = {
	value = 0
}

sul_tribes_share_pct = {
	value = 0
}

sul_tribes_tax_income = {
	value = 0
}

sul_tribes_rents_wages = {
	value = 0
}

# --- Debug helpers ---

sul_debug_loc_pop = {
	value = population
}

sul_debug_num_locations = {
	value = num_locations
}
