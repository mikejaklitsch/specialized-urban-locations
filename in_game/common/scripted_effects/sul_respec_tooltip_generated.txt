# AUTO-GENERATED by tools/generate_respec_tooltips.py
# Do not hand-edit. Regenerate after changing sul_is_*_building triggers.

sul_respec_show_destroy_non_mining = {
	if = { limit = { has_building = building_type:apothecary } custom_tooltip = SUL_RESPEC_DESTROY_apothecary }

	if = { limit = { has_building = building_type:beer_workshop } custom_tooltip = SUL_RESPEC_DESTROY_beer_workshop }

	if = { limit = { has_building = building_type:brewery } custom_tooltip = SUL_RESPEC_DESTROY_brewery }

	if = {
		limit = { has_building = building_type:brewery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_brewery_manufactory
	}

	if = { limit = { has_building = building_type:brewery_mill } custom_tooltip = SUL_RESPEC_DESTROY_brewery_mill }

	if = { limit = { has_building = building_type:charcoal_maker } custom_tooltip = SUL_RESPEC_DESTROY_charcoal_maker }

	if = { limit = { has_building = building_type:clay_pit } custom_tooltip = SUL_RESPEC_DESTROY_clay_pit }

	if = { limit = { has_building = building_type:clearing_house } custom_tooltip = SUL_RESPEC_DESTROY_clearing_house }

	if = { limit = { has_building = building_type:cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_cloth_guild }

	if = {
		limit = { has_building = building_type:cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_cloth_manufactory
	}

	if = { limit = { has_building = building_type:cloth_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cloth_workshop }

	if = { limit = { has_building = building_type:commerce_center } custom_tooltip = SUL_RESPEC_DESTROY_commerce_center }

	if = {
		limit = { has_building = building_type:cotton_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_cotton_plantation
	}

	if = { limit = { has_building = building_type:customs_house } custom_tooltip = SUL_RESPEC_DESTROY_customs_house }

	if = {
		limit = { has_building = building_type:distiller_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_distiller_manufactory
	}

	if = { limit = { has_building = building_type:distiller_mill } custom_tooltip = SUL_RESPEC_DESTROY_distiller_mill }

	if = { limit = { has_building = building_type:distillers_guild } custom_tooltip = SUL_RESPEC_DESTROY_distillers_guild }

	if = {
		limit = { has_building = building_type:distillers_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_distillers_workshop
	}

	if = { limit = { has_building = building_type:dyes_guild } custom_tooltip = SUL_RESPEC_DESTROY_dyes_guild }

	if = { limit = { has_building = building_type:dyes_mill } custom_tooltip = SUL_RESPEC_DESTROY_dyes_mill }

	if = { limit = { has_building = building_type:dyes_workshop } custom_tooltip = SUL_RESPEC_DESTROY_dyes_workshop }

	if = { limit = { has_building = building_type:dyesworks } custom_tooltip = SUL_RESPEC_DESTROY_dyesworks }

	if = {
		limit = { has_building = building_type:elephant_hunting_grounds }
		custom_tooltip = SUL_RESPEC_DESTROY_elephant_hunting_grounds
	}

	if = { limit = { has_building = building_type:entrepot } custom_tooltip = SUL_RESPEC_DESTROY_entrepot }

	if = { limit = { has_building = building_type:farming_village } custom_tooltip = SUL_RESPEC_DESTROY_farming_village }

	if = { limit = { has_building = building_type:fiber_crops_farm } custom_tooltip = SUL_RESPEC_DESTROY_fiber_crops_farm }

	if = { limit = { has_building = building_type:fine_cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_guild }

	if = {
		limit = { has_building = building_type:fine_cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_manufactory
	}

	if = { limit = { has_building = building_type:fine_cloth_mill } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_mill }

	if = {
		limit = { has_building = building_type:fine_cloth_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_workshop
	}

	if = { limit = { has_building = building_type:fishing_village } custom_tooltip = SUL_RESPEC_DESTROY_fishing_village }

	if = { limit = { has_building = building_type:forest_village } custom_tooltip = SUL_RESPEC_DESTROY_forest_village }

	if = { limit = { has_building = building_type:fruit_orchard } custom_tooltip = SUL_RESPEC_DESTROY_fruit_orchard }

	if = { limit = { has_building = building_type:funduq } custom_tooltip = SUL_RESPEC_DESTROY_funduq }

	if = { limit = { has_building = building_type:furniture_guild } custom_tooltip = SUL_RESPEC_DESTROY_furniture_guild }

	if = {
		limit = { has_building = building_type:furniture_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_manufactory
	}

	if = { limit = { has_building = building_type:furniture_mill } custom_tooltip = SUL_RESPEC_DESTROY_furniture_mill }

	if = {
		limit = { has_building = building_type:furniture_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_workshop
	}

	if = { limit = { has_building = building_type:glass_guild } custom_tooltip = SUL_RESPEC_DESTROY_glass_guild }

	if = { limit = { has_building = building_type:glass_mill } custom_tooltip = SUL_RESPEC_DESTROY_glass_mill }

	if = { limit = { has_building = building_type:glass_workshop } custom_tooltip = SUL_RESPEC_DESTROY_glass_workshop }

	if = { limit = { has_building = building_type:glassworks } custom_tooltip = SUL_RESPEC_DESTROY_glassworks }

	if = {
		limit = { has_building = building_type:grand_marketplace }
		custom_tooltip = SUL_RESPEC_DESTROY_grand_marketplace
	}

	if = { limit = { has_building = building_type:horse_breeders } custom_tooltip = SUL_RESPEC_DESTROY_horse_breeders }

	if = {
		limit = { has_building = building_type:improved_charcoal_maker }
		custom_tooltip = SUL_RESPEC_DESTROY_improved_charcoal_maker
	}

	if = {
		limit = { has_building = building_type:irrigation_systems }
		custom_tooltip = SUL_RESPEC_DESTROY_irrigation_systems
	}

	if = {
		limit = { has_building = building_type:lacquerware_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_guild
	}

	if = {
		limit = { has_building = building_type:lacquerware_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_manufactory
	}

	if = { limit = { has_building = building_type:lumber_mill } custom_tooltip = SUL_RESPEC_DESTROY_lumber_mill }

	if = { limit = { has_building = building_type:market_village } custom_tooltip = SUL_RESPEC_DESTROY_market_village }

	if = { limit = { has_building = building_type:market_warehouse } custom_tooltip = SUL_RESPEC_DESTROY_market_warehouse }

	if = { limit = { has_building = building_type:marketplace } custom_tooltip = SUL_RESPEC_DESTROY_marketplace }

	if = {
		limit = { has_building = building_type:merchants_quarters }
		custom_tooltip = SUL_RESPEC_DESTROY_merchants_quarters
	}

	if = {
		limit = { has_building = building_type:naval_supplies_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_guild
	}

	if = {
		limit = { has_building = building_type:naval_supplies_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_manufactory
	}

	if = {
		limit = { has_building = building_type:naval_supplies_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_mill
	}

	if = {
		limit = { has_building = building_type:naval_supplies_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_workshop
	}

	if = { limit = { has_building = building_type:paper_guild } custom_tooltip = SUL_RESPEC_DESTROY_paper_guild }

	if = {
		limit = { has_building = building_type:paper_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_paper_manufactory
	}

	if = { limit = { has_building = building_type:paper_mill } custom_tooltip = SUL_RESPEC_DESTROY_paper_mill }

	if = { limit = { has_building = building_type:paper_workshop } custom_tooltip = SUL_RESPEC_DESTROY_paper_workshop }

	if = { limit = { has_building = building_type:perfumery } custom_tooltip = SUL_RESPEC_DESTROY_perfumery }

	if = { limit = { has_building = building_type:porcelain_guild } custom_tooltip = SUL_RESPEC_DESTROY_porcelain_guild }

	if = {
		limit = { has_building = building_type:porcelain_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_porcelain_manufactory
	}

	if = { limit = { has_building = building_type:port_authority } custom_tooltip = SUL_RESPEC_DESTROY_port_authority }

	if = { limit = { has_building = building_type:pottery_guild } custom_tooltip = SUL_RESPEC_DESTROY_pottery_guild }

	if = {
		limit = { has_building = building_type:pottery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_pottery_manufactory
	}

	if = { limit = { has_building = building_type:pottery_mill } custom_tooltip = SUL_RESPEC_DESTROY_pottery_mill }

	if = { limit = { has_building = building_type:pottery_workshop } custom_tooltip = SUL_RESPEC_DESTROY_pottery_workshop }

	if = {
		limit = { has_building = building_type:printing_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_manufactory
	}

	if = { limit = { has_building = building_type:printing_mill } custom_tooltip = SUL_RESPEC_DESTROY_printing_mill }

	if = {
		limit = { has_building = building_type:printing_press_shop }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_press_shop
	}

	if = {
		limit = { has_building = building_type:putrefaction_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_mill
	}

	if = {
		limit = { has_building = building_type:putrefaction_works }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_works
	}

	if = { limit = { has_building = building_type:rural_clothmaker } custom_tooltip = SUL_RESPEC_DESTROY_rural_clothmaker }

	if = { limit = { has_building = building_type:salt_collector } custom_tooltip = SUL_RESPEC_DESTROY_salt_collector }

	if = { limit = { has_building = building_type:saltpeter_guild } custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_guild }

	if = {
		limit = { has_building = building_type:saltpeter_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_workshop
	}

	if = { limit = { has_building = building_type:sand_pit } custom_tooltip = SUL_RESPEC_DESTROY_sand_pit }

	if = { limit = { has_building = building_type:sawmill } custom_tooltip = SUL_RESPEC_DESTROY_sawmill }

	if = { limit = { has_building = building_type:scriptorium } custom_tooltip = SUL_RESPEC_DESTROY_scriptorium }

	if = { limit = { has_building = building_type:sheep_farms } custom_tooltip = SUL_RESPEC_DESTROY_sheep_farms }

	if = { limit = { has_building = building_type:stock_exchange } custom_tooltip = SUL_RESPEC_DESTROY_stock_exchange }

	if = { limit = { has_building = building_type:sugar_plantation } custom_tooltip = SUL_RESPEC_DESTROY_sugar_plantation }

	if = {
		limit = { has_building = building_type:sul_farmers_market }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_farmers_market
	}

	if = {
		limit = { has_building = building_type:sul_fishing_ground }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_fishing_ground
	}

	if = { limit = { has_building = building_type:sul_legume_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_legume_field }

	if = { limit = { has_building = building_type:sul_maize_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_maize_field }

	if = { limit = { has_building = building_type:sul_millet_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_millet_field }

	if = { limit = { has_building = building_type:sul_olive_grove } custom_tooltip = SUL_RESPEC_DESTROY_sul_olive_grove }

	if = { limit = { has_building = building_type:sul_pastureland } custom_tooltip = SUL_RESPEC_DESTROY_sul_pastureland }

	if = { limit = { has_building = building_type:sul_potato_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_potato_field }

	if = { limit = { has_building = building_type:sul_rice_paddy } custom_tooltip = SUL_RESPEC_DESTROY_sul_rice_paddy }

	if = { limit = { has_building = building_type:sul_rural_apiary } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_apiary }

	if = { limit = { has_building = building_type:sul_rural_brewer } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_brewer }

	if = {
		limit = { has_building = building_type:sul_rural_carpenter }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_carpenter
	}

	if = {
		limit = { has_building = building_type:sul_rural_distiller }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_distiller
	}

	if = {
		limit = { has_building = building_type:sul_rural_glassmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_glassmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_herbalist }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_herbalist
	}

	if = {
		limit = { has_building = building_type:sul_rural_papermaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_papermaker
	}

	if = { limit = { has_building = building_type:sul_rural_potter } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_potter }

	if = {
		limit = { has_building = building_type:sul_rural_ropemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_ropemaker
	}

	if = { limit = { has_building = building_type:sul_rural_tanner } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_tanner }

	if = {
		limit = { has_building = building_type:sul_rural_winemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_winemaker
	}

	if = { limit = { has_building = building_type:sul_wheat_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_wheat_field }

	if = { limit = { has_building = building_type:tannery } custom_tooltip = SUL_RESPEC_DESTROY_tannery }

	if = {
		limit = { has_building = building_type:tannery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_tannery_manufactory
	}

	if = { limit = { has_building = building_type:tannery_mill } custom_tooltip = SUL_RESPEC_DESTROY_tannery_mill }

	if = { limit = { has_building = building_type:tanning_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tanning_workshop }

	if = { limit = { has_building = building_type:tar_kiln } custom_tooltip = SUL_RESPEC_DESTROY_tar_kiln }

	if = { limit = { has_building = building_type:textile_mill } custom_tooltip = SUL_RESPEC_DESTROY_textile_mill }

	if = {
		limit = { has_building = building_type:tobacco_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_tobacco_plantation
	}

	if = { limit = { has_building = building_type:trading_hub } custom_tooltip = SUL_RESPEC_DESTROY_trading_hub }

	if = { limit = { has_building = building_type:university } custom_tooltip = SUL_RESPEC_DESTROY_university }

	if = { limit = { has_building = building_type:windmill } custom_tooltip = SUL_RESPEC_DESTROY_windmill }

	if = { limit = { has_building = building_type:winery } custom_tooltip = SUL_RESPEC_DESTROY_winery }

	if = {
		limit = { has_building = building_type:winery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_winery_manufactory
	}
}

sul_respec_show_destroy_non_farming = {
	if = { limit = { has_building = building_type:apothecary } custom_tooltip = SUL_RESPEC_DESTROY_apothecary }

	if = { limit = { has_building = building_type:bog_iron_smelter } custom_tooltip = SUL_RESPEC_DESTROY_bog_iron_smelter }

	if = { limit = { has_building = building_type:cannon_foundry } custom_tooltip = SUL_RESPEC_DESTROY_cannon_foundry }

	if = { limit = { has_building = building_type:cannon_maker } custom_tooltip = SUL_RESPEC_DESTROY_cannon_maker }

	if = { limit = { has_building = building_type:cannon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cannon_workshop }

	if = { limit = { has_building = building_type:cannons_factory } custom_tooltip = SUL_RESPEC_DESTROY_cannons_factory }

	if = { limit = { has_building = building_type:charcoal_maker } custom_tooltip = SUL_RESPEC_DESTROY_charcoal_maker }

	if = { limit = { has_building = building_type:clay_pit } custom_tooltip = SUL_RESPEC_DESTROY_clay_pit }

	if = { limit = { has_building = building_type:clearing_house } custom_tooltip = SUL_RESPEC_DESTROY_clearing_house }

	if = { limit = { has_building = building_type:cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_cloth_guild }

	if = {
		limit = { has_building = building_type:cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_cloth_manufactory
	}

	if = { limit = { has_building = building_type:cloth_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cloth_workshop }

	if = { limit = { has_building = building_type:commerce_center } custom_tooltip = SUL_RESPEC_DESTROY_commerce_center }

	if = { limit = { has_building = building_type:customs_house } custom_tooltip = SUL_RESPEC_DESTROY_customs_house }

	if = { limit = { has_building = building_type:dyes_guild } custom_tooltip = SUL_RESPEC_DESTROY_dyes_guild }

	if = { limit = { has_building = building_type:dyes_mill } custom_tooltip = SUL_RESPEC_DESTROY_dyes_mill }

	if = { limit = { has_building = building_type:dyes_workshop } custom_tooltip = SUL_RESPEC_DESTROY_dyes_workshop }

	if = { limit = { has_building = building_type:dyesworks } custom_tooltip = SUL_RESPEC_DESTROY_dyesworks }

	if = {
		limit = { has_building = building_type:elephant_hunting_grounds }
		custom_tooltip = SUL_RESPEC_DESTROY_elephant_hunting_grounds
	}

	if = { limit = { has_building = building_type:entrepot } custom_tooltip = SUL_RESPEC_DESTROY_entrepot }

	if = { limit = { has_building = building_type:fine_cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_guild }

	if = {
		limit = { has_building = building_type:fine_cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_manufactory
	}

	if = { limit = { has_building = building_type:fine_cloth_mill } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_mill }

	if = {
		limit = { has_building = building_type:fine_cloth_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_workshop
	}

	if = { limit = { has_building = building_type:firearms_factory } custom_tooltip = SUL_RESPEC_DESTROY_firearms_factory }

	if = {
		limit = { has_building = building_type:firearms_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_firearms_manufactory
	}

	if = { limit = { has_building = building_type:fishing_village } custom_tooltip = SUL_RESPEC_DESTROY_fishing_village }

	if = { limit = { has_building = building_type:forest_village } custom_tooltip = SUL_RESPEC_DESTROY_forest_village }

	if = { limit = { has_building = building_type:funduq } custom_tooltip = SUL_RESPEC_DESTROY_funduq }

	if = { limit = { has_building = building_type:furniture_guild } custom_tooltip = SUL_RESPEC_DESTROY_furniture_guild }

	if = {
		limit = { has_building = building_type:furniture_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_manufactory
	}

	if = { limit = { has_building = building_type:furniture_mill } custom_tooltip = SUL_RESPEC_DESTROY_furniture_mill }

	if = {
		limit = { has_building = building_type:furniture_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_workshop
	}

	if = { limit = { has_building = building_type:glass_guild } custom_tooltip = SUL_RESPEC_DESTROY_glass_guild }

	if = { limit = { has_building = building_type:glass_mill } custom_tooltip = SUL_RESPEC_DESTROY_glass_mill }

	if = { limit = { has_building = building_type:glass_workshop } custom_tooltip = SUL_RESPEC_DESTROY_glass_workshop }

	if = { limit = { has_building = building_type:glassworks } custom_tooltip = SUL_RESPEC_DESTROY_glassworks }

	if = {
		limit = { has_building = building_type:grand_marketplace }
		custom_tooltip = SUL_RESPEC_DESTROY_grand_marketplace
	}

	if = { limit = { has_building = building_type:gun_smith } custom_tooltip = SUL_RESPEC_DESTROY_gun_smith }

	if = { limit = { has_building = building_type:guns_workshop } custom_tooltip = SUL_RESPEC_DESTROY_guns_workshop }

	if = {
		limit = { has_building = building_type:hand_cannon_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_hand_cannon_guild
	}

	if = {
		limit = { has_building = building_type:improved_charcoal_maker }
		custom_tooltip = SUL_RESPEC_DESTROY_improved_charcoal_maker
	}

	if = { limit = { has_building = building_type:iron_foundry } custom_tooltip = SUL_RESPEC_DESTROY_iron_foundry }

	if = { limit = { has_building = building_type:iron_mill } custom_tooltip = SUL_RESPEC_DESTROY_iron_mill }

	if = { limit = { has_building = building_type:jewelry_guild } custom_tooltip = SUL_RESPEC_DESTROY_jewelry_guild }

	if = {
		limit = { has_building = building_type:lacquerware_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_guild
	}

	if = {
		limit = { has_building = building_type:lacquerware_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_manufactory
	}

	if = { limit = { has_building = building_type:local_smelters } custom_tooltip = SUL_RESPEC_DESTROY_local_smelters }

	if = { limit = { has_building = building_type:lumber_mill } custom_tooltip = SUL_RESPEC_DESTROY_lumber_mill }

	if = { limit = { has_building = building_type:market_village } custom_tooltip = SUL_RESPEC_DESTROY_market_village }

	if = { limit = { has_building = building_type:market_warehouse } custom_tooltip = SUL_RESPEC_DESTROY_market_warehouse }

	if = { limit = { has_building = building_type:marketplace } custom_tooltip = SUL_RESPEC_DESTROY_marketplace }

	if = {
		limit = { has_building = building_type:merchants_quarters }
		custom_tooltip = SUL_RESPEC_DESTROY_merchants_quarters
	}

	if = { limit = { has_building = building_type:mercury_patio } custom_tooltip = SUL_RESPEC_DESTROY_mercury_patio }

	if = {
		limit = { has_building = building_type:naval_supplies_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_guild
	}

	if = {
		limit = { has_building = building_type:naval_supplies_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_manufactory
	}

	if = {
		limit = { has_building = building_type:naval_supplies_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_mill
	}

	if = {
		limit = { has_building = building_type:naval_supplies_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_workshop
	}

	if = { limit = { has_building = building_type:paper_guild } custom_tooltip = SUL_RESPEC_DESTROY_paper_guild }

	if = {
		limit = { has_building = building_type:paper_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_paper_manufactory
	}

	if = { limit = { has_building = building_type:paper_mill } custom_tooltip = SUL_RESPEC_DESTROY_paper_mill }

	if = { limit = { has_building = building_type:paper_workshop } custom_tooltip = SUL_RESPEC_DESTROY_paper_workshop }

	if = { limit = { has_building = building_type:perfumery } custom_tooltip = SUL_RESPEC_DESTROY_perfumery }

	if = { limit = { has_building = building_type:porcelain_guild } custom_tooltip = SUL_RESPEC_DESTROY_porcelain_guild }

	if = {
		limit = { has_building = building_type:porcelain_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_porcelain_manufactory
	}

	if = { limit = { has_building = building_type:port_authority } custom_tooltip = SUL_RESPEC_DESTROY_port_authority }

	if = { limit = { has_building = building_type:pottery_guild } custom_tooltip = SUL_RESPEC_DESTROY_pottery_guild }

	if = {
		limit = { has_building = building_type:pottery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_pottery_manufactory
	}

	if = { limit = { has_building = building_type:pottery_mill } custom_tooltip = SUL_RESPEC_DESTROY_pottery_mill }

	if = { limit = { has_building = building_type:pottery_workshop } custom_tooltip = SUL_RESPEC_DESTROY_pottery_workshop }

	if = {
		limit = { has_building = building_type:printing_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_manufactory
	}

	if = { limit = { has_building = building_type:printing_mill } custom_tooltip = SUL_RESPEC_DESTROY_printing_mill }

	if = {
		limit = { has_building = building_type:printing_press_shop }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_press_shop
	}

	if = {
		limit = { has_building = building_type:putrefaction_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_mill
	}

	if = {
		limit = { has_building = building_type:putrefaction_works }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_works
	}

	if = { limit = { has_building = building_type:rural_clothmaker } custom_tooltip = SUL_RESPEC_DESTROY_rural_clothmaker }

	if = { limit = { has_building = building_type:salt_collector } custom_tooltip = SUL_RESPEC_DESTROY_salt_collector }

	if = { limit = { has_building = building_type:saltpeter_guild } custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_guild }

	if = {
		limit = { has_building = building_type:saltpeter_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_workshop
	}

	if = { limit = { has_building = building_type:sand_pit } custom_tooltip = SUL_RESPEC_DESTROY_sand_pit }

	if = { limit = { has_building = building_type:sawmill } custom_tooltip = SUL_RESPEC_DESTROY_sawmill }

	if = { limit = { has_building = building_type:scriptorium } custom_tooltip = SUL_RESPEC_DESTROY_scriptorium }

	if = { limit = { has_building = building_type:sheep_farms } custom_tooltip = SUL_RESPEC_DESTROY_sheep_farms }

	if = { limit = { has_building = building_type:steel_mill } custom_tooltip = SUL_RESPEC_DESTROY_steel_mill }

	if = { limit = { has_building = building_type:stock_exchange } custom_tooltip = SUL_RESPEC_DESTROY_stock_exchange }

	if = { limit = { has_building = building_type:stone_quarry } custom_tooltip = SUL_RESPEC_DESTROY_stone_quarry }

	if = {
		limit = { has_building = building_type:sul_fishing_ground }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_fishing_ground
	}

	if = { limit = { has_building = building_type:sul_iron_mine } custom_tooltip = SUL_RESPEC_DESTROY_sul_iron_mine }

	if = {
		limit = { has_building = building_type:sul_marble_quarry }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_marble_quarry
	}

	if = {
		limit = { has_building = building_type:sul_mining_village }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_mining_village
	}

	if = { limit = { has_building = building_type:sul_rural_apiary } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_apiary }

	if = {
		limit = { has_building = building_type:sul_rural_blacksmith }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_blacksmith
	}

	if = {
		limit = { has_building = building_type:sul_rural_carpenter }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_carpenter
	}

	if = {
		limit = { has_building = building_type:sul_rural_glassmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_glassmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_herbalist }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_herbalist
	}

	if = {
		limit = { has_building = building_type:sul_rural_jeweler }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_jeweler
	}

	if = {
		limit = { has_building = building_type:sul_rural_papermaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_papermaker
	}

	if = { limit = { has_building = building_type:sul_rural_potter } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_potter }

	if = {
		limit = { has_building = building_type:sul_rural_ropemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_ropemaker
	}

	if = { limit = { has_building = building_type:sul_rural_tanner } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_tanner }

	if = {
		limit = { has_building = building_type:sul_rural_weaponmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_weaponmaker
	}

	if = { limit = { has_building = building_type:tannery } custom_tooltip = SUL_RESPEC_DESTROY_tannery }

	if = {
		limit = { has_building = building_type:tannery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_tannery_manufactory
	}

	if = { limit = { has_building = building_type:tannery_mill } custom_tooltip = SUL_RESPEC_DESTROY_tannery_mill }

	if = { limit = { has_building = building_type:tanning_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tanning_workshop }

	if = { limit = { has_building = building_type:tar_kiln } custom_tooltip = SUL_RESPEC_DESTROY_tar_kiln }

	if = { limit = { has_building = building_type:textile_mill } custom_tooltip = SUL_RESPEC_DESTROY_textile_mill }

	if = { limit = { has_building = building_type:tools_guild } custom_tooltip = SUL_RESPEC_DESTROY_tools_guild }

	if = { limit = { has_building = building_type:tools_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tools_workshop }

	if = { limit = { has_building = building_type:trading_hub } custom_tooltip = SUL_RESPEC_DESTROY_trading_hub }

	if = { limit = { has_building = building_type:university } custom_tooltip = SUL_RESPEC_DESTROY_university }

	if = { limit = { has_building = building_type:weapon_factory } custom_tooltip = SUL_RESPEC_DESTROY_weapon_factory }

	if = { limit = { has_building = building_type:weapon_guild } custom_tooltip = SUL_RESPEC_DESTROY_weapon_guild }

	if = {
		limit = { has_building = building_type:weapon_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_weapon_manufactory
	}

	if = { limit = { has_building = building_type:weapon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_weapon_workshop }
}

sul_respec_show_destroy_non_gathering = {
	if = { limit = { has_building = building_type:beer_workshop } custom_tooltip = SUL_RESPEC_DESTROY_beer_workshop }

	if = { limit = { has_building = building_type:bog_iron_smelter } custom_tooltip = SUL_RESPEC_DESTROY_bog_iron_smelter }

	if = { limit = { has_building = building_type:brewery } custom_tooltip = SUL_RESPEC_DESTROY_brewery }

	if = {
		limit = { has_building = building_type:brewery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_brewery_manufactory
	}

	if = { limit = { has_building = building_type:brewery_mill } custom_tooltip = SUL_RESPEC_DESTROY_brewery_mill }

	if = { limit = { has_building = building_type:cannon_foundry } custom_tooltip = SUL_RESPEC_DESTROY_cannon_foundry }

	if = { limit = { has_building = building_type:cannon_maker } custom_tooltip = SUL_RESPEC_DESTROY_cannon_maker }

	if = { limit = { has_building = building_type:cannon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cannon_workshop }

	if = { limit = { has_building = building_type:cannons_factory } custom_tooltip = SUL_RESPEC_DESTROY_cannons_factory }

	if = { limit = { has_building = building_type:charcoal_maker } custom_tooltip = SUL_RESPEC_DESTROY_charcoal_maker }

	if = { limit = { has_building = building_type:clearing_house } custom_tooltip = SUL_RESPEC_DESTROY_clearing_house }

	if = { limit = { has_building = building_type:cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_cloth_guild }

	if = {
		limit = { has_building = building_type:cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_cloth_manufactory
	}

	if = { limit = { has_building = building_type:cloth_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cloth_workshop }

	if = { limit = { has_building = building_type:commerce_center } custom_tooltip = SUL_RESPEC_DESTROY_commerce_center }

	if = {
		limit = { has_building = building_type:cotton_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_cotton_plantation
	}

	if = { limit = { has_building = building_type:customs_house } custom_tooltip = SUL_RESPEC_DESTROY_customs_house }

	if = {
		limit = { has_building = building_type:distiller_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_distiller_manufactory
	}

	if = { limit = { has_building = building_type:distiller_mill } custom_tooltip = SUL_RESPEC_DESTROY_distiller_mill }

	if = { limit = { has_building = building_type:distillers_guild } custom_tooltip = SUL_RESPEC_DESTROY_distillers_guild }

	if = {
		limit = { has_building = building_type:distillers_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_distillers_workshop
	}

	if = { limit = { has_building = building_type:dyes_guild } custom_tooltip = SUL_RESPEC_DESTROY_dyes_guild }

	if = { limit = { has_building = building_type:dyes_mill } custom_tooltip = SUL_RESPEC_DESTROY_dyes_mill }

	if = { limit = { has_building = building_type:dyes_workshop } custom_tooltip = SUL_RESPEC_DESTROY_dyes_workshop }

	if = { limit = { has_building = building_type:dyesworks } custom_tooltip = SUL_RESPEC_DESTROY_dyesworks }

	if = {
		limit = { has_building = building_type:elephant_hunting_grounds }
		custom_tooltip = SUL_RESPEC_DESTROY_elephant_hunting_grounds
	}

	if = { limit = { has_building = building_type:entrepot } custom_tooltip = SUL_RESPEC_DESTROY_entrepot }

	if = { limit = { has_building = building_type:farming_village } custom_tooltip = SUL_RESPEC_DESTROY_farming_village }

	if = { limit = { has_building = building_type:fiber_crops_farm } custom_tooltip = SUL_RESPEC_DESTROY_fiber_crops_farm }

	if = { limit = { has_building = building_type:fine_cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_guild }

	if = {
		limit = { has_building = building_type:fine_cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_manufactory
	}

	if = { limit = { has_building = building_type:fine_cloth_mill } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_mill }

	if = {
		limit = { has_building = building_type:fine_cloth_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_workshop
	}

	if = { limit = { has_building = building_type:firearms_factory } custom_tooltip = SUL_RESPEC_DESTROY_firearms_factory }

	if = {
		limit = { has_building = building_type:firearms_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_firearms_manufactory
	}

	if = { limit = { has_building = building_type:forest_village } custom_tooltip = SUL_RESPEC_DESTROY_forest_village }

	if = { limit = { has_building = building_type:fruit_orchard } custom_tooltip = SUL_RESPEC_DESTROY_fruit_orchard }

	if = { limit = { has_building = building_type:funduq } custom_tooltip = SUL_RESPEC_DESTROY_funduq }

	if = { limit = { has_building = building_type:furniture_guild } custom_tooltip = SUL_RESPEC_DESTROY_furniture_guild }

	if = {
		limit = { has_building = building_type:furniture_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_manufactory
	}

	if = { limit = { has_building = building_type:furniture_mill } custom_tooltip = SUL_RESPEC_DESTROY_furniture_mill }

	if = {
		limit = { has_building = building_type:furniture_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_workshop
	}

	if = {
		limit = { has_building = building_type:grand_marketplace }
		custom_tooltip = SUL_RESPEC_DESTROY_grand_marketplace
	}

	if = { limit = { has_building = building_type:gun_smith } custom_tooltip = SUL_RESPEC_DESTROY_gun_smith }

	if = { limit = { has_building = building_type:guns_workshop } custom_tooltip = SUL_RESPEC_DESTROY_guns_workshop }

	if = {
		limit = { has_building = building_type:hand_cannon_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_hand_cannon_guild
	}

	if = { limit = { has_building = building_type:horse_breeders } custom_tooltip = SUL_RESPEC_DESTROY_horse_breeders }

	if = {
		limit = { has_building = building_type:improved_charcoal_maker }
		custom_tooltip = SUL_RESPEC_DESTROY_improved_charcoal_maker
	}

	if = { limit = { has_building = building_type:iron_foundry } custom_tooltip = SUL_RESPEC_DESTROY_iron_foundry }

	if = { limit = { has_building = building_type:iron_mill } custom_tooltip = SUL_RESPEC_DESTROY_iron_mill }

	if = {
		limit = { has_building = building_type:irrigation_systems }
		custom_tooltip = SUL_RESPEC_DESTROY_irrigation_systems
	}

	if = { limit = { has_building = building_type:jewelry_guild } custom_tooltip = SUL_RESPEC_DESTROY_jewelry_guild }

	if = {
		limit = { has_building = building_type:lacquerware_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_guild
	}

	if = {
		limit = { has_building = building_type:lacquerware_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_manufactory
	}

	if = { limit = { has_building = building_type:local_smelters } custom_tooltip = SUL_RESPEC_DESTROY_local_smelters }

	if = { limit = { has_building = building_type:lumber_mill } custom_tooltip = SUL_RESPEC_DESTROY_lumber_mill }

	if = { limit = { has_building = building_type:market_village } custom_tooltip = SUL_RESPEC_DESTROY_market_village }

	if = { limit = { has_building = building_type:market_warehouse } custom_tooltip = SUL_RESPEC_DESTROY_market_warehouse }

	if = { limit = { has_building = building_type:marketplace } custom_tooltip = SUL_RESPEC_DESTROY_marketplace }

	if = {
		limit = { has_building = building_type:merchants_quarters }
		custom_tooltip = SUL_RESPEC_DESTROY_merchants_quarters
	}

	if = { limit = { has_building = building_type:mercury_patio } custom_tooltip = SUL_RESPEC_DESTROY_mercury_patio }

	if = {
		limit = { has_building = building_type:naval_supplies_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_guild
	}

	if = {
		limit = { has_building = building_type:naval_supplies_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_manufactory
	}

	if = {
		limit = { has_building = building_type:naval_supplies_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_mill
	}

	if = {
		limit = { has_building = building_type:naval_supplies_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_workshop
	}

	if = { limit = { has_building = building_type:paper_guild } custom_tooltip = SUL_RESPEC_DESTROY_paper_guild }

	if = {
		limit = { has_building = building_type:paper_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_paper_manufactory
	}

	if = { limit = { has_building = building_type:paper_mill } custom_tooltip = SUL_RESPEC_DESTROY_paper_mill }

	if = { limit = { has_building = building_type:paper_workshop } custom_tooltip = SUL_RESPEC_DESTROY_paper_workshop }

	if = { limit = { has_building = building_type:perfumery } custom_tooltip = SUL_RESPEC_DESTROY_perfumery }

	if = { limit = { has_building = building_type:port_authority } custom_tooltip = SUL_RESPEC_DESTROY_port_authority }

	if = {
		limit = { has_building = building_type:printing_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_manufactory
	}

	if = { limit = { has_building = building_type:printing_mill } custom_tooltip = SUL_RESPEC_DESTROY_printing_mill }

	if = {
		limit = { has_building = building_type:printing_press_shop }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_press_shop
	}

	if = { limit = { has_building = building_type:rural_clothmaker } custom_tooltip = SUL_RESPEC_DESTROY_rural_clothmaker }

	if = { limit = { has_building = building_type:sawmill } custom_tooltip = SUL_RESPEC_DESTROY_sawmill }

	if = { limit = { has_building = building_type:scriptorium } custom_tooltip = SUL_RESPEC_DESTROY_scriptorium }

	if = { limit = { has_building = building_type:steel_mill } custom_tooltip = SUL_RESPEC_DESTROY_steel_mill }

	if = { limit = { has_building = building_type:stock_exchange } custom_tooltip = SUL_RESPEC_DESTROY_stock_exchange }

	if = { limit = { has_building = building_type:stone_quarry } custom_tooltip = SUL_RESPEC_DESTROY_stone_quarry }

	if = { limit = { has_building = building_type:sugar_plantation } custom_tooltip = SUL_RESPEC_DESTROY_sugar_plantation }

	if = {
		limit = { has_building = building_type:sul_farmers_market }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_farmers_market
	}

	if = { limit = { has_building = building_type:sul_iron_mine } custom_tooltip = SUL_RESPEC_DESTROY_sul_iron_mine }

	if = { limit = { has_building = building_type:sul_legume_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_legume_field }

	if = { limit = { has_building = building_type:sul_maize_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_maize_field }

	if = {
		limit = { has_building = building_type:sul_marble_quarry }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_marble_quarry
	}

	if = { limit = { has_building = building_type:sul_millet_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_millet_field }

	if = {
		limit = { has_building = building_type:sul_mining_village }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_mining_village
	}

	if = { limit = { has_building = building_type:sul_olive_grove } custom_tooltip = SUL_RESPEC_DESTROY_sul_olive_grove }

	if = { limit = { has_building = building_type:sul_pastureland } custom_tooltip = SUL_RESPEC_DESTROY_sul_pastureland }

	if = { limit = { has_building = building_type:sul_potato_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_potato_field }

	if = { limit = { has_building = building_type:sul_rice_paddy } custom_tooltip = SUL_RESPEC_DESTROY_sul_rice_paddy }

	if = { limit = { has_building = building_type:sul_rural_apiary } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_apiary }

	if = {
		limit = { has_building = building_type:sul_rural_blacksmith }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_blacksmith
	}

	if = { limit = { has_building = building_type:sul_rural_brewer } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_brewer }

	if = {
		limit = { has_building = building_type:sul_rural_carpenter }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_carpenter
	}

	if = {
		limit = { has_building = building_type:sul_rural_distiller }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_distiller
	}

	if = {
		limit = { has_building = building_type:sul_rural_jeweler }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_jeweler
	}

	if = {
		limit = { has_building = building_type:sul_rural_papermaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_papermaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_ropemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_ropemaker
	}

	if = { limit = { has_building = building_type:sul_rural_tanner } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_tanner }

	if = {
		limit = { has_building = building_type:sul_rural_weaponmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_weaponmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_winemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_winemaker
	}

	if = { limit = { has_building = building_type:sul_wheat_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_wheat_field }

	if = { limit = { has_building = building_type:tannery } custom_tooltip = SUL_RESPEC_DESTROY_tannery }

	if = {
		limit = { has_building = building_type:tannery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_tannery_manufactory
	}

	if = { limit = { has_building = building_type:tannery_mill } custom_tooltip = SUL_RESPEC_DESTROY_tannery_mill }

	if = { limit = { has_building = building_type:tanning_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tanning_workshop }

	if = { limit = { has_building = building_type:tar_kiln } custom_tooltip = SUL_RESPEC_DESTROY_tar_kiln }

	if = { limit = { has_building = building_type:textile_mill } custom_tooltip = SUL_RESPEC_DESTROY_textile_mill }

	if = {
		limit = { has_building = building_type:tobacco_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_tobacco_plantation
	}

	if = { limit = { has_building = building_type:tools_guild } custom_tooltip = SUL_RESPEC_DESTROY_tools_guild }

	if = { limit = { has_building = building_type:tools_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tools_workshop }

	if = { limit = { has_building = building_type:trading_hub } custom_tooltip = SUL_RESPEC_DESTROY_trading_hub }

	if = { limit = { has_building = building_type:university } custom_tooltip = SUL_RESPEC_DESTROY_university }

	if = { limit = { has_building = building_type:weapon_factory } custom_tooltip = SUL_RESPEC_DESTROY_weapon_factory }

	if = { limit = { has_building = building_type:weapon_guild } custom_tooltip = SUL_RESPEC_DESTROY_weapon_guild }

	if = {
		limit = { has_building = building_type:weapon_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_weapon_manufactory
	}

	if = { limit = { has_building = building_type:weapon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_weapon_workshop }

	if = { limit = { has_building = building_type:windmill } custom_tooltip = SUL_RESPEC_DESTROY_windmill }

	if = { limit = { has_building = building_type:winery } custom_tooltip = SUL_RESPEC_DESTROY_winery }

	if = {
		limit = { has_building = building_type:winery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_winery_manufactory
	}
}

sul_respec_show_destroy_non_woodland = {
	if = { limit = { has_building = building_type:apothecary } custom_tooltip = SUL_RESPEC_DESTROY_apothecary }

	if = { limit = { has_building = building_type:beer_workshop } custom_tooltip = SUL_RESPEC_DESTROY_beer_workshop }

	if = { limit = { has_building = building_type:bog_iron_smelter } custom_tooltip = SUL_RESPEC_DESTROY_bog_iron_smelter }

	if = { limit = { has_building = building_type:brewery } custom_tooltip = SUL_RESPEC_DESTROY_brewery }

	if = {
		limit = { has_building = building_type:brewery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_brewery_manufactory
	}

	if = { limit = { has_building = building_type:brewery_mill } custom_tooltip = SUL_RESPEC_DESTROY_brewery_mill }

	if = { limit = { has_building = building_type:cannon_foundry } custom_tooltip = SUL_RESPEC_DESTROY_cannon_foundry }

	if = { limit = { has_building = building_type:cannon_maker } custom_tooltip = SUL_RESPEC_DESTROY_cannon_maker }

	if = { limit = { has_building = building_type:cannon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cannon_workshop }

	if = { limit = { has_building = building_type:cannons_factory } custom_tooltip = SUL_RESPEC_DESTROY_cannons_factory }

	if = { limit = { has_building = building_type:clay_pit } custom_tooltip = SUL_RESPEC_DESTROY_clay_pit }

	if = { limit = { has_building = building_type:clearing_house } custom_tooltip = SUL_RESPEC_DESTROY_clearing_house }

	if = { limit = { has_building = building_type:cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_cloth_guild }

	if = {
		limit = { has_building = building_type:cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_cloth_manufactory
	}

	if = { limit = { has_building = building_type:cloth_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cloth_workshop }

	if = { limit = { has_building = building_type:commerce_center } custom_tooltip = SUL_RESPEC_DESTROY_commerce_center }

	if = {
		limit = { has_building = building_type:cotton_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_cotton_plantation
	}

	if = { limit = { has_building = building_type:customs_house } custom_tooltip = SUL_RESPEC_DESTROY_customs_house }

	if = {
		limit = { has_building = building_type:distiller_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_distiller_manufactory
	}

	if = { limit = { has_building = building_type:distiller_mill } custom_tooltip = SUL_RESPEC_DESTROY_distiller_mill }

	if = { limit = { has_building = building_type:distillers_guild } custom_tooltip = SUL_RESPEC_DESTROY_distillers_guild }

	if = {
		limit = { has_building = building_type:distillers_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_distillers_workshop
	}

	if = { limit = { has_building = building_type:entrepot } custom_tooltip = SUL_RESPEC_DESTROY_entrepot }

	if = { limit = { has_building = building_type:farming_village } custom_tooltip = SUL_RESPEC_DESTROY_farming_village }

	if = { limit = { has_building = building_type:fiber_crops_farm } custom_tooltip = SUL_RESPEC_DESTROY_fiber_crops_farm }

	if = { limit = { has_building = building_type:fine_cloth_guild } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_guild }

	if = {
		limit = { has_building = building_type:fine_cloth_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_manufactory
	}

	if = { limit = { has_building = building_type:fine_cloth_mill } custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_mill }

	if = {
		limit = { has_building = building_type:fine_cloth_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_fine_cloth_workshop
	}

	if = { limit = { has_building = building_type:firearms_factory } custom_tooltip = SUL_RESPEC_DESTROY_firearms_factory }

	if = {
		limit = { has_building = building_type:firearms_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_firearms_manufactory
	}

	if = { limit = { has_building = building_type:fishing_village } custom_tooltip = SUL_RESPEC_DESTROY_fishing_village }

	if = { limit = { has_building = building_type:fruit_orchard } custom_tooltip = SUL_RESPEC_DESTROY_fruit_orchard }

	if = { limit = { has_building = building_type:funduq } custom_tooltip = SUL_RESPEC_DESTROY_funduq }

	if = { limit = { has_building = building_type:glass_guild } custom_tooltip = SUL_RESPEC_DESTROY_glass_guild }

	if = { limit = { has_building = building_type:glass_mill } custom_tooltip = SUL_RESPEC_DESTROY_glass_mill }

	if = { limit = { has_building = building_type:glass_workshop } custom_tooltip = SUL_RESPEC_DESTROY_glass_workshop }

	if = { limit = { has_building = building_type:glassworks } custom_tooltip = SUL_RESPEC_DESTROY_glassworks }

	if = {
		limit = { has_building = building_type:grand_marketplace }
		custom_tooltip = SUL_RESPEC_DESTROY_grand_marketplace
	}

	if = { limit = { has_building = building_type:gun_smith } custom_tooltip = SUL_RESPEC_DESTROY_gun_smith }

	if = { limit = { has_building = building_type:guns_workshop } custom_tooltip = SUL_RESPEC_DESTROY_guns_workshop }

	if = {
		limit = { has_building = building_type:hand_cannon_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_hand_cannon_guild
	}

	if = { limit = { has_building = building_type:horse_breeders } custom_tooltip = SUL_RESPEC_DESTROY_horse_breeders }

	if = { limit = { has_building = building_type:iron_foundry } custom_tooltip = SUL_RESPEC_DESTROY_iron_foundry }

	if = { limit = { has_building = building_type:iron_mill } custom_tooltip = SUL_RESPEC_DESTROY_iron_mill }

	if = {
		limit = { has_building = building_type:irrigation_systems }
		custom_tooltip = SUL_RESPEC_DESTROY_irrigation_systems
	}

	if = { limit = { has_building = building_type:jewelry_guild } custom_tooltip = SUL_RESPEC_DESTROY_jewelry_guild }

	if = { limit = { has_building = building_type:local_smelters } custom_tooltip = SUL_RESPEC_DESTROY_local_smelters }

	if = { limit = { has_building = building_type:market_village } custom_tooltip = SUL_RESPEC_DESTROY_market_village }

	if = { limit = { has_building = building_type:market_warehouse } custom_tooltip = SUL_RESPEC_DESTROY_market_warehouse }

	if = { limit = { has_building = building_type:marketplace } custom_tooltip = SUL_RESPEC_DESTROY_marketplace }

	if = {
		limit = { has_building = building_type:merchants_quarters }
		custom_tooltip = SUL_RESPEC_DESTROY_merchants_quarters
	}

	if = { limit = { has_building = building_type:mercury_patio } custom_tooltip = SUL_RESPEC_DESTROY_mercury_patio }

	if = {
		limit = { has_building = building_type:naval_supplies_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_guild
	}

	if = {
		limit = { has_building = building_type:naval_supplies_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_manufactory
	}

	if = {
		limit = { has_building = building_type:naval_supplies_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_mill
	}

	if = {
		limit = { has_building = building_type:naval_supplies_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_naval_supplies_workshop
	}

	if = { limit = { has_building = building_type:perfumery } custom_tooltip = SUL_RESPEC_DESTROY_perfumery }

	if = { limit = { has_building = building_type:porcelain_guild } custom_tooltip = SUL_RESPEC_DESTROY_porcelain_guild }

	if = {
		limit = { has_building = building_type:porcelain_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_porcelain_manufactory
	}

	if = { limit = { has_building = building_type:port_authority } custom_tooltip = SUL_RESPEC_DESTROY_port_authority }

	if = { limit = { has_building = building_type:pottery_guild } custom_tooltip = SUL_RESPEC_DESTROY_pottery_guild }

	if = {
		limit = { has_building = building_type:pottery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_pottery_manufactory
	}

	if = { limit = { has_building = building_type:pottery_mill } custom_tooltip = SUL_RESPEC_DESTROY_pottery_mill }

	if = { limit = { has_building = building_type:pottery_workshop } custom_tooltip = SUL_RESPEC_DESTROY_pottery_workshop }

	if = {
		limit = { has_building = building_type:printing_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_manufactory
	}

	if = { limit = { has_building = building_type:printing_mill } custom_tooltip = SUL_RESPEC_DESTROY_printing_mill }

	if = {
		limit = { has_building = building_type:printing_press_shop }
		custom_tooltip = SUL_RESPEC_DESTROY_printing_press_shop
	}

	if = {
		limit = { has_building = building_type:putrefaction_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_mill
	}

	if = {
		limit = { has_building = building_type:putrefaction_works }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_works
	}

	if = { limit = { has_building = building_type:rural_clothmaker } custom_tooltip = SUL_RESPEC_DESTROY_rural_clothmaker }

	if = { limit = { has_building = building_type:salt_collector } custom_tooltip = SUL_RESPEC_DESTROY_salt_collector }

	if = { limit = { has_building = building_type:saltpeter_guild } custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_guild }

	if = {
		limit = { has_building = building_type:saltpeter_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_workshop
	}

	if = { limit = { has_building = building_type:sand_pit } custom_tooltip = SUL_RESPEC_DESTROY_sand_pit }

	if = { limit = { has_building = building_type:scriptorium } custom_tooltip = SUL_RESPEC_DESTROY_scriptorium }

	if = { limit = { has_building = building_type:sheep_farms } custom_tooltip = SUL_RESPEC_DESTROY_sheep_farms }

	if = { limit = { has_building = building_type:steel_mill } custom_tooltip = SUL_RESPEC_DESTROY_steel_mill }

	if = { limit = { has_building = building_type:stock_exchange } custom_tooltip = SUL_RESPEC_DESTROY_stock_exchange }

	if = { limit = { has_building = building_type:stone_quarry } custom_tooltip = SUL_RESPEC_DESTROY_stone_quarry }

	if = { limit = { has_building = building_type:sugar_plantation } custom_tooltip = SUL_RESPEC_DESTROY_sugar_plantation }

	if = {
		limit = { has_building = building_type:sul_farmers_market }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_farmers_market
	}

	if = {
		limit = { has_building = building_type:sul_fishing_ground }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_fishing_ground
	}

	if = { limit = { has_building = building_type:sul_iron_mine } custom_tooltip = SUL_RESPEC_DESTROY_sul_iron_mine }

	if = { limit = { has_building = building_type:sul_legume_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_legume_field }

	if = { limit = { has_building = building_type:sul_maize_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_maize_field }

	if = {
		limit = { has_building = building_type:sul_marble_quarry }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_marble_quarry
	}

	if = { limit = { has_building = building_type:sul_millet_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_millet_field }

	if = {
		limit = { has_building = building_type:sul_mining_village }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_mining_village
	}

	if = { limit = { has_building = building_type:sul_olive_grove } custom_tooltip = SUL_RESPEC_DESTROY_sul_olive_grove }

	if = { limit = { has_building = building_type:sul_pastureland } custom_tooltip = SUL_RESPEC_DESTROY_sul_pastureland }

	if = { limit = { has_building = building_type:sul_potato_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_potato_field }

	if = { limit = { has_building = building_type:sul_rice_paddy } custom_tooltip = SUL_RESPEC_DESTROY_sul_rice_paddy }

	if = {
		limit = { has_building = building_type:sul_rural_blacksmith }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_blacksmith
	}

	if = { limit = { has_building = building_type:sul_rural_brewer } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_brewer }

	if = {
		limit = { has_building = building_type:sul_rural_distiller }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_distiller
	}

	if = {
		limit = { has_building = building_type:sul_rural_glassmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_glassmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_herbalist }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_herbalist
	}

	if = {
		limit = { has_building = building_type:sul_rural_jeweler }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_jeweler
	}

	if = { limit = { has_building = building_type:sul_rural_potter } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_potter }

	if = {
		limit = { has_building = building_type:sul_rural_ropemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_ropemaker
	}

	if = { limit = { has_building = building_type:sul_rural_tanner } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_tanner }

	if = {
		limit = { has_building = building_type:sul_rural_weaponmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_weaponmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_winemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_winemaker
	}

	if = { limit = { has_building = building_type:sul_wheat_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_wheat_field }

	if = { limit = { has_building = building_type:textile_mill } custom_tooltip = SUL_RESPEC_DESTROY_textile_mill }

	if = {
		limit = { has_building = building_type:tobacco_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_tobacco_plantation
	}

	if = { limit = { has_building = building_type:tools_guild } custom_tooltip = SUL_RESPEC_DESTROY_tools_guild }

	if = { limit = { has_building = building_type:tools_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tools_workshop }

	if = { limit = { has_building = building_type:trading_hub } custom_tooltip = SUL_RESPEC_DESTROY_trading_hub }

	if = { limit = { has_building = building_type:university } custom_tooltip = SUL_RESPEC_DESTROY_university }

	if = { limit = { has_building = building_type:weapon_factory } custom_tooltip = SUL_RESPEC_DESTROY_weapon_factory }

	if = { limit = { has_building = building_type:weapon_guild } custom_tooltip = SUL_RESPEC_DESTROY_weapon_guild }

	if = {
		limit = { has_building = building_type:weapon_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_weapon_manufactory
	}

	if = { limit = { has_building = building_type:weapon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_weapon_workshop }

	if = { limit = { has_building = building_type:windmill } custom_tooltip = SUL_RESPEC_DESTROY_windmill }

	if = { limit = { has_building = building_type:winery } custom_tooltip = SUL_RESPEC_DESTROY_winery }

	if = {
		limit = { has_building = building_type:winery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_winery_manufactory
	}
}

sul_respec_show_destroy_non_commercial = {
	if = { limit = { has_building = building_type:apothecary } custom_tooltip = SUL_RESPEC_DESTROY_apothecary }

	if = { limit = { has_building = building_type:beer_workshop } custom_tooltip = SUL_RESPEC_DESTROY_beer_workshop }

	if = { limit = { has_building = building_type:bog_iron_smelter } custom_tooltip = SUL_RESPEC_DESTROY_bog_iron_smelter }

	if = { limit = { has_building = building_type:brewery } custom_tooltip = SUL_RESPEC_DESTROY_brewery }

	if = {
		limit = { has_building = building_type:brewery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_brewery_manufactory
	}

	if = { limit = { has_building = building_type:brewery_mill } custom_tooltip = SUL_RESPEC_DESTROY_brewery_mill }

	if = { limit = { has_building = building_type:cannon_foundry } custom_tooltip = SUL_RESPEC_DESTROY_cannon_foundry }

	if = { limit = { has_building = building_type:cannon_maker } custom_tooltip = SUL_RESPEC_DESTROY_cannon_maker }

	if = { limit = { has_building = building_type:cannon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_cannon_workshop }

	if = { limit = { has_building = building_type:cannons_factory } custom_tooltip = SUL_RESPEC_DESTROY_cannons_factory }

	if = { limit = { has_building = building_type:charcoal_maker } custom_tooltip = SUL_RESPEC_DESTROY_charcoal_maker }

	if = { limit = { has_building = building_type:clay_pit } custom_tooltip = SUL_RESPEC_DESTROY_clay_pit }

	if = {
		limit = { has_building = building_type:cotton_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_cotton_plantation
	}

	if = {
		limit = { has_building = building_type:distiller_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_distiller_manufactory
	}

	if = { limit = { has_building = building_type:distiller_mill } custom_tooltip = SUL_RESPEC_DESTROY_distiller_mill }

	if = { limit = { has_building = building_type:distillers_guild } custom_tooltip = SUL_RESPEC_DESTROY_distillers_guild }

	if = {
		limit = { has_building = building_type:distillers_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_distillers_workshop
	}

	if = { limit = { has_building = building_type:dyes_guild } custom_tooltip = SUL_RESPEC_DESTROY_dyes_guild }

	if = { limit = { has_building = building_type:dyes_mill } custom_tooltip = SUL_RESPEC_DESTROY_dyes_mill }

	if = { limit = { has_building = building_type:dyes_workshop } custom_tooltip = SUL_RESPEC_DESTROY_dyes_workshop }

	if = { limit = { has_building = building_type:dyesworks } custom_tooltip = SUL_RESPEC_DESTROY_dyesworks }

	if = {
		limit = { has_building = building_type:elephant_hunting_grounds }
		custom_tooltip = SUL_RESPEC_DESTROY_elephant_hunting_grounds
	}

	if = { limit = { has_building = building_type:farming_village } custom_tooltip = SUL_RESPEC_DESTROY_farming_village }

	if = { limit = { has_building = building_type:fiber_crops_farm } custom_tooltip = SUL_RESPEC_DESTROY_fiber_crops_farm }

	if = { limit = { has_building = building_type:firearms_factory } custom_tooltip = SUL_RESPEC_DESTROY_firearms_factory }

	if = {
		limit = { has_building = building_type:firearms_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_firearms_manufactory
	}

	if = { limit = { has_building = building_type:fishing_village } custom_tooltip = SUL_RESPEC_DESTROY_fishing_village }

	if = { limit = { has_building = building_type:forest_village } custom_tooltip = SUL_RESPEC_DESTROY_forest_village }

	if = { limit = { has_building = building_type:fruit_orchard } custom_tooltip = SUL_RESPEC_DESTROY_fruit_orchard }

	if = { limit = { has_building = building_type:furniture_guild } custom_tooltip = SUL_RESPEC_DESTROY_furniture_guild }

	if = {
		limit = { has_building = building_type:furniture_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_manufactory
	}

	if = { limit = { has_building = building_type:furniture_mill } custom_tooltip = SUL_RESPEC_DESTROY_furniture_mill }

	if = {
		limit = { has_building = building_type:furniture_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_furniture_workshop
	}

	if = { limit = { has_building = building_type:glass_guild } custom_tooltip = SUL_RESPEC_DESTROY_glass_guild }

	if = { limit = { has_building = building_type:glass_mill } custom_tooltip = SUL_RESPEC_DESTROY_glass_mill }

	if = { limit = { has_building = building_type:glass_workshop } custom_tooltip = SUL_RESPEC_DESTROY_glass_workshop }

	if = { limit = { has_building = building_type:glassworks } custom_tooltip = SUL_RESPEC_DESTROY_glassworks }

	if = { limit = { has_building = building_type:gun_smith } custom_tooltip = SUL_RESPEC_DESTROY_gun_smith }

	if = { limit = { has_building = building_type:guns_workshop } custom_tooltip = SUL_RESPEC_DESTROY_guns_workshop }

	if = {
		limit = { has_building = building_type:hand_cannon_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_hand_cannon_guild
	}

	if = { limit = { has_building = building_type:horse_breeders } custom_tooltip = SUL_RESPEC_DESTROY_horse_breeders }

	if = {
		limit = { has_building = building_type:improved_charcoal_maker }
		custom_tooltip = SUL_RESPEC_DESTROY_improved_charcoal_maker
	}

	if = { limit = { has_building = building_type:iron_foundry } custom_tooltip = SUL_RESPEC_DESTROY_iron_foundry }

	if = { limit = { has_building = building_type:iron_mill } custom_tooltip = SUL_RESPEC_DESTROY_iron_mill }

	if = {
		limit = { has_building = building_type:irrigation_systems }
		custom_tooltip = SUL_RESPEC_DESTROY_irrigation_systems
	}

	if = { limit = { has_building = building_type:jewelry_guild } custom_tooltip = SUL_RESPEC_DESTROY_jewelry_guild }

	if = {
		limit = { has_building = building_type:lacquerware_guild }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_guild
	}

	if = {
		limit = { has_building = building_type:lacquerware_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_lacquerware_manufactory
	}

	if = { limit = { has_building = building_type:local_smelters } custom_tooltip = SUL_RESPEC_DESTROY_local_smelters }

	if = { limit = { has_building = building_type:lumber_mill } custom_tooltip = SUL_RESPEC_DESTROY_lumber_mill }

	if = { limit = { has_building = building_type:mercury_patio } custom_tooltip = SUL_RESPEC_DESTROY_mercury_patio }

	if = { limit = { has_building = building_type:paper_guild } custom_tooltip = SUL_RESPEC_DESTROY_paper_guild }

	if = {
		limit = { has_building = building_type:paper_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_paper_manufactory
	}

	if = { limit = { has_building = building_type:paper_mill } custom_tooltip = SUL_RESPEC_DESTROY_paper_mill }

	if = { limit = { has_building = building_type:paper_workshop } custom_tooltip = SUL_RESPEC_DESTROY_paper_workshop }

	if = { limit = { has_building = building_type:porcelain_guild } custom_tooltip = SUL_RESPEC_DESTROY_porcelain_guild }

	if = {
		limit = { has_building = building_type:porcelain_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_porcelain_manufactory
	}

	if = { limit = { has_building = building_type:pottery_guild } custom_tooltip = SUL_RESPEC_DESTROY_pottery_guild }

	if = {
		limit = { has_building = building_type:pottery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_pottery_manufactory
	}

	if = { limit = { has_building = building_type:pottery_mill } custom_tooltip = SUL_RESPEC_DESTROY_pottery_mill }

	if = { limit = { has_building = building_type:pottery_workshop } custom_tooltip = SUL_RESPEC_DESTROY_pottery_workshop }

	if = {
		limit = { has_building = building_type:putrefaction_mill }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_mill
	}

	if = {
		limit = { has_building = building_type:putrefaction_works }
		custom_tooltip = SUL_RESPEC_DESTROY_putrefaction_works
	}

	if = { limit = { has_building = building_type:salt_collector } custom_tooltip = SUL_RESPEC_DESTROY_salt_collector }

	if = { limit = { has_building = building_type:saltpeter_guild } custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_guild }

	if = {
		limit = { has_building = building_type:saltpeter_workshop }
		custom_tooltip = SUL_RESPEC_DESTROY_saltpeter_workshop
	}

	if = { limit = { has_building = building_type:sand_pit } custom_tooltip = SUL_RESPEC_DESTROY_sand_pit }

	if = { limit = { has_building = building_type:sawmill } custom_tooltip = SUL_RESPEC_DESTROY_sawmill }

	if = { limit = { has_building = building_type:sheep_farms } custom_tooltip = SUL_RESPEC_DESTROY_sheep_farms }

	if = { limit = { has_building = building_type:steel_mill } custom_tooltip = SUL_RESPEC_DESTROY_steel_mill }

	if = { limit = { has_building = building_type:stone_quarry } custom_tooltip = SUL_RESPEC_DESTROY_stone_quarry }

	if = { limit = { has_building = building_type:sugar_plantation } custom_tooltip = SUL_RESPEC_DESTROY_sugar_plantation }

	if = {
		limit = { has_building = building_type:sul_farmers_market }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_farmers_market
	}

	if = {
		limit = { has_building = building_type:sul_fishing_ground }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_fishing_ground
	}

	if = { limit = { has_building = building_type:sul_iron_mine } custom_tooltip = SUL_RESPEC_DESTROY_sul_iron_mine }

	if = { limit = { has_building = building_type:sul_legume_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_legume_field }

	if = { limit = { has_building = building_type:sul_maize_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_maize_field }

	if = {
		limit = { has_building = building_type:sul_marble_quarry }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_marble_quarry
	}

	if = { limit = { has_building = building_type:sul_millet_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_millet_field }

	if = {
		limit = { has_building = building_type:sul_mining_village }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_mining_village
	}

	if = { limit = { has_building = building_type:sul_olive_grove } custom_tooltip = SUL_RESPEC_DESTROY_sul_olive_grove }

	if = { limit = { has_building = building_type:sul_pastureland } custom_tooltip = SUL_RESPEC_DESTROY_sul_pastureland }

	if = { limit = { has_building = building_type:sul_potato_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_potato_field }

	if = { limit = { has_building = building_type:sul_rice_paddy } custom_tooltip = SUL_RESPEC_DESTROY_sul_rice_paddy }

	if = { limit = { has_building = building_type:sul_rural_apiary } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_apiary }

	if = {
		limit = { has_building = building_type:sul_rural_blacksmith }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_blacksmith
	}

	if = { limit = { has_building = building_type:sul_rural_brewer } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_brewer }

	if = {
		limit = { has_building = building_type:sul_rural_carpenter }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_carpenter
	}

	if = {
		limit = { has_building = building_type:sul_rural_distiller }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_distiller
	}

	if = {
		limit = { has_building = building_type:sul_rural_glassmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_glassmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_herbalist }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_herbalist
	}

	if = {
		limit = { has_building = building_type:sul_rural_jeweler }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_jeweler
	}

	if = {
		limit = { has_building = building_type:sul_rural_papermaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_papermaker
	}

	if = { limit = { has_building = building_type:sul_rural_potter } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_potter }

	if = { limit = { has_building = building_type:sul_rural_tanner } custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_tanner }

	if = {
		limit = { has_building = building_type:sul_rural_weaponmaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_weaponmaker
	}

	if = {
		limit = { has_building = building_type:sul_rural_winemaker }
		custom_tooltip = SUL_RESPEC_DESTROY_sul_rural_winemaker
	}

	if = { limit = { has_building = building_type:sul_wheat_field } custom_tooltip = SUL_RESPEC_DESTROY_sul_wheat_field }

	if = { limit = { has_building = building_type:tannery } custom_tooltip = SUL_RESPEC_DESTROY_tannery }

	if = {
		limit = { has_building = building_type:tannery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_tannery_manufactory
	}

	if = { limit = { has_building = building_type:tannery_mill } custom_tooltip = SUL_RESPEC_DESTROY_tannery_mill }

	if = { limit = { has_building = building_type:tanning_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tanning_workshop }

	if = { limit = { has_building = building_type:tar_kiln } custom_tooltip = SUL_RESPEC_DESTROY_tar_kiln }

	if = {
		limit = { has_building = building_type:tobacco_plantation }
		custom_tooltip = SUL_RESPEC_DESTROY_tobacco_plantation
	}

	if = { limit = { has_building = building_type:tools_guild } custom_tooltip = SUL_RESPEC_DESTROY_tools_guild }

	if = { limit = { has_building = building_type:tools_workshop } custom_tooltip = SUL_RESPEC_DESTROY_tools_workshop }

	if = { limit = { has_building = building_type:weapon_factory } custom_tooltip = SUL_RESPEC_DESTROY_weapon_factory }

	if = { limit = { has_building = building_type:weapon_guild } custom_tooltip = SUL_RESPEC_DESTROY_weapon_guild }

	if = {
		limit = { has_building = building_type:weapon_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_weapon_manufactory
	}

	if = { limit = { has_building = building_type:weapon_workshop } custom_tooltip = SUL_RESPEC_DESTROY_weapon_workshop }

	if = { limit = { has_building = building_type:windmill } custom_tooltip = SUL_RESPEC_DESTROY_windmill }

	if = { limit = { has_building = building_type:winery } custom_tooltip = SUL_RESPEC_DESTROY_winery }

	if = {
		limit = { has_building = building_type:winery_manufactory }
		custom_tooltip = SUL_RESPEC_DESTROY_winery_manufactory
	}
}
