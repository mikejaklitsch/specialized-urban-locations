# AI locations eligible for economy update, ordered by staleness

sul_stale_ai_location = {
	base = location_in_the_world

	conditions = {
		has_owner = yes
		owner = { is_ai = yes is_real_country = yes }
		market_access > 0
	}
}

# Locations eligible for respec score caching, ordered by staleness

sul_respec_cache_location = {
	base = location_in_the_world

	conditions = {
		has_owner = yes
	}
}
