# Locations eligible for WPP batch update, ordered by staleness.
# Includes player and AI — enrichment requires periodic WPP recalc.

sul_stale_ai_location = {
	base = location_in_the_world

	conditions = {
		has_owner = yes
		owner = { is_real_country = yes }
		market_access > 0
	}
}
