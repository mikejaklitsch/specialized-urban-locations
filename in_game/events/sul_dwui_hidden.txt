# Intentionally orphaned event to satisfy the engine's static variable analyzer.
# These variables are consumed by GUI but the analyzer cannot detect GUI reads.

namespace = sul_dwui_hidden

sul_dwui_hidden.1 = {
	type = country_event
	orphan = yes
	title = "sul_dwui_hidden.1.t"
	desc = "sul_dwui_hidden.1.d"

	trigger = { always = no }

	immediate = {
		# "set but never used" — consumed by GUI piecharts / filter expressions
		change_variable = { name = dwui_regular_str multiply = 1 }
		change_variable = { name = dwui_merc_str multiply = 1 }
		change_variable = { name = dwui_levy_str multiply = 1 }
		change_variable = { name = dwui_battle_filter multiply = 1 }
	}

	option = { name = "OK" }
}
