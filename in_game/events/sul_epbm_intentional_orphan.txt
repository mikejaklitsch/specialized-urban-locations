# Intentionally orphaned event to satisfy the engine's static variable analyzer.
# These variables are used in localization/GUI but the analyzer can't detect
# localization refs and flags them as "never set" or "set but never used".
# This event is never triggered.

namespace = sul_epbm_intentional_orphan

sul_epbm_intentional_orphan.1 = {
	type = country_event
	title = "sul_epbm_intentional_orphan.1.t"
	desc = "sul_epbm_intentional_orphan.1.d"

	trigger = { always = no }

	immediate = {
		# ── Routing vars: "used but never set" in script (set dynamically from map) ──
		set_global_variable = {
			name = sul_epbm_estate_ref
			value = estate_type:nobles_estate
		}

		# ── Display vars: "set but never used" (consumed by GUI/localization) ──
		change_variable = { name = sul_epbm_show_nobles multiply = 1 }
		change_variable = { name = sul_epbm_show_clergy multiply = 1 }
		change_variable = { name = sul_epbm_show_burghers multiply = 1 }
		change_variable = { name = sul_epbm_show_peasants multiply = 1 }
		change_variable = { name = sul_epbm_show_dhimmi multiply = 1 }
		change_variable = { name = sul_epbm_show_tribes multiply = 1 }
		change_variable = { name = sul_epbm_show_cossacks multiply = 1 }
	}

	option = { name = "OK" }
}
