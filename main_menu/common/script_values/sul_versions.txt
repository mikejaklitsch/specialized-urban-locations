# SUL version stamps — one integer per subsystem. Single source of truth.
#
# When you make a save-breaking change to a subsystem, bump its value here.
# On next load, the matching version check on weather_monthly_pulse will
# detect the mismatch (saved global variable < this value) and trigger a
# full re-init of ONLY that subsystem. Untouched subsystems keep their
# saved state and do not re-init — so don't bump values you didn't change.

sul_specialization_version_value = 10

sul_war_version_value = 210

sul_minting_version_value = 101

sul_integration_version_value = 100

sul_epbm_version_value = 21
