# Bouvet Island marker: engine strips this when the mod is removed, so its
# absence on re-add triggers a full re-init.

sul_minting_active = {
	game_data = { category = location }
}

# AI minting modifiers — applied via monthly pulse with dynamic size since
# auto_modifier evaluation is too expensive at AI scale.

sul_minting_ai_minting = {
	game_data = { category = country }

	minting_income_factor = 1.0
}

sul_minting_ai_inflation = {
	game_data = { category = country }

	monthly_inflation = 1.0
}

sul_minting_ai_rebasement = {
	game_data = { category = country }

	monthly_gold_income = -1.0
}
