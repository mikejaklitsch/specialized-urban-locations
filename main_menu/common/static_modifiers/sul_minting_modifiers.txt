# The Gold Standard - Modifiers
# Mod state marker: applied to Bouvet Island during initialization.
# Stripped by the engine when mod is removed (definition disappears).
# Absence triggers full re-init on re-add.

sul_minting_active = {
	game_data = { category = location }
}

# AI minting modifiers — applied via monthly pulse with dynamic size.
# Base value × size = effective modifier.

sul_minting_ai_minting = {
	game_data = { category = country }

	minting_income_factor = 1.0
}

sul_minting_ai_inflation = {
	game_data = { category = country }

	monthly_inflation = 1.0
}

sul_minting_ai_rebasement = {
	game_data = { category = country }

	monthly_gold_income = -1.0
}
